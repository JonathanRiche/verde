# Machine-local Agent CI secrets. Copy values into .env.agent-ci when you need
# to run workflows that require private GitHub secrets.

# Required only for .github/workflows/aur-sync.yml.
AUR_SSH_PRIVATE_KEY=
