// Generated by `mise run mobile-models-generate`. DO NOT EDIT.
import Foundation

private enum ModelDiscriminator: String, CodingKey { case type }

// Opaque JSON is limited to log fields and K-06's currently empty collections.
// Integer cases precede Decimal so 64-bit values never pass through Double.
indirect enum JSONValue: Codable {
    case null, bool(Bool), string(String), integer(Int64), unsigned(UInt64)
    case decimal(Decimal), array([JSONValue]), object([String: JSONValue])

    init(from decoder: Decoder) throws {
        let c = try decoder.singleValueContainer()
        if c.decodeNil() { self = .null }
        else if let v = try? c.decode(Bool.self) { self = .bool(v) }
        else if let v = try? c.decode(String.self) { self = .string(v) }
        else if let v = try? c.decode(Int64.self) { self = .integer(v) }
        else if let v = try? c.decode(UInt64.self) { self = .unsigned(v) }
        else if let v = try? c.decode(Decimal.self) { self = .decimal(v) }
        else if let v = try? c.decode([JSONValue].self) { self = .array(v) }
        else { self = .object(try c.decode([String: JSONValue].self)) }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.singleValueContainer()
        switch self {
        case .null: try c.encodeNil()
        case .bool(let v): try c.encode(v)
        case .string(let v): try c.encode(v)
        case .integer(let v): try c.encode(v)
        case .unsigned(let v): try c.encode(v)
        case .decimal(let v): try c.encode(v)
        case .array(let v): try c.encode(v)
        case .object(let v): try c.encode(v)
        }
    }
}

struct TerminalConfig: Codable {
    var `api_version`: UInt32 = 1
    var `cols`: UInt16
    var `rows`: UInt16
    var `scrollback_rows`: UInt32
}

extension TerminalConfig {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `cols`
        case `rows`
        case `scrollback_rows`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`cols` = try c.decode(UInt16.self, forKey: .`cols`)
        self.`rows` = try c.decode(UInt16.self, forKey: .`rows`)
        self.`scrollback_rows` = try c.decode(UInt32.self, forKey: .`scrollback_rows`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`cols`, forKey: .`cols`)
        try c.encode(self.`rows`, forKey: .`rows`)
        try c.encode(self.`scrollback_rows`, forKey: .`scrollback_rows`)
    }
}

enum TerminalCursorShape: String, Codable {
    case `block`
    case `underline`
    case `bar`
}

struct TerminalCursor: Codable {
    var `row`: UInt16
    var `col`: UInt16
    var `visible`: Bool
    var `shape`: TerminalCursorShape
}

extension TerminalCursor {
    private enum CodingKeys: String, CodingKey {
        case `row`
        case `col`
        case `visible`
        case `shape`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`row` = try c.decode(UInt16.self, forKey: .`row`)
        self.`col` = try c.decode(UInt16.self, forKey: .`col`)
        self.`visible` = try c.decode(Bool.self, forKey: .`visible`)
        self.`shape` = try c.decode(TerminalCursorShape.self, forKey: .`shape`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`row`, forKey: .`row`)
        try c.encode(self.`col`, forKey: .`col`)
        try c.encode(self.`visible`, forKey: .`visible`)
        try c.encode(self.`shape`, forKey: .`shape`)
    }
}

struct TerminalCell: Codable {
    var `text`: String
    var `width`: UInt8
    var `fg`: String
    var `bg`: String
    var `bold`: Bool
    var `italic`: Bool
    var `underline`: Bool
    var `strikethrough`: Bool
    var `inverse`: Bool
}

extension TerminalCell {
    private enum CodingKeys: String, CodingKey {
        case `text`
        case `width`
        case `fg`
        case `bg`
        case `bold`
        case `italic`
        case `underline`
        case `strikethrough`
        case `inverse`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`text` = try c.decode(String.self, forKey: .`text`)
        self.`width` = try c.decode(UInt8.self, forKey: .`width`)
        self.`fg` = try c.decode(String.self, forKey: .`fg`)
        self.`bg` = try c.decode(String.self, forKey: .`bg`)
        self.`bold` = try c.decode(Bool.self, forKey: .`bold`)
        self.`italic` = try c.decode(Bool.self, forKey: .`italic`)
        self.`underline` = try c.decode(Bool.self, forKey: .`underline`)
        self.`strikethrough` = try c.decode(Bool.self, forKey: .`strikethrough`)
        self.`inverse` = try c.decode(Bool.self, forKey: .`inverse`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`text`, forKey: .`text`)
        try c.encode(self.`width`, forKey: .`width`)
        try c.encode(self.`fg`, forKey: .`fg`)
        try c.encode(self.`bg`, forKey: .`bg`)
        try c.encode(self.`bold`, forKey: .`bold`)
        try c.encode(self.`italic`, forKey: .`italic`)
        try c.encode(self.`underline`, forKey: .`underline`)
        try c.encode(self.`strikethrough`, forKey: .`strikethrough`)
        try c.encode(self.`inverse`, forKey: .`inverse`)
    }
}

struct TerminalSnapshot: Codable {
    var `api_version`: UInt32 = 1
    var `revision`: String
    var `cols`: UInt16
    var `rows`: UInt16
    var `scroll_offset`: UInt32
    var `scrollback_rows`: UInt32
    var `reply_bytes_base64`: String
    var `reverse_video`: Bool
    var `vt_modes`: VtModes
    var `cursor`: TerminalCursor
    var `cells`: [TerminalCell]
    var `title`: String
}

extension TerminalSnapshot {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `cols`
        case `rows`
        case `scroll_offset`
        case `scrollback_rows`
        case `reply_bytes_base64`
        case `reverse_video`
        case `vt_modes`
        case `cursor`
        case `cells`
        case `title`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`cols` = try c.decode(UInt16.self, forKey: .`cols`)
        self.`rows` = try c.decode(UInt16.self, forKey: .`rows`)
        self.`scroll_offset` = try c.decode(UInt32.self, forKey: .`scroll_offset`)
        self.`scrollback_rows` = try c.decode(UInt32.self, forKey: .`scrollback_rows`)
        self.`reply_bytes_base64` = try c.decode(String.self, forKey: .`reply_bytes_base64`)
        self.`reverse_video` = try c.decode(Bool.self, forKey: .`reverse_video`)
        self.`vt_modes` = try c.decode(VtModes.self, forKey: .`vt_modes`)
        self.`cursor` = try c.decode(TerminalCursor.self, forKey: .`cursor`)
        self.`cells` = try c.decode([TerminalCell].self, forKey: .`cells`)
        self.`title` = try c.decode(String.self, forKey: .`title`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`cols`, forKey: .`cols`)
        try c.encode(self.`rows`, forKey: .`rows`)
        try c.encode(self.`scroll_offset`, forKey: .`scroll_offset`)
        try c.encode(self.`scrollback_rows`, forKey: .`scrollback_rows`)
        try c.encode(self.`reply_bytes_base64`, forKey: .`reply_bytes_base64`)
        try c.encode(self.`reverse_video`, forKey: .`reverse_video`)
        try c.encode(self.`vt_modes`, forKey: .`vt_modes`)
        try c.encode(self.`cursor`, forKey: .`cursor`)
        try c.encode(self.`cells`, forKey: .`cells`)
        try c.encode(self.`title`, forKey: .`title`)
    }
}

struct TerminalView: Codable {
    var `terminal_id`: String
    var `workspace_id`: String = ""
    var `label`: String = "Terminal"
    var `session_status`: String = "running"
    var `attached`: Bool = false
    var `cols`: UInt16 = 80
    var `rows`: UInt16 = 24
    var `next_offset`: String? = nil
    var `grid_revision`: String = "0"
    var `stale`: Bool = true
    var `error`: LocalError? = nil
}

extension TerminalView {
    private enum CodingKeys: String, CodingKey {
        case `terminal_id`
        case `workspace_id`
        case `label`
        case `session_status`
        case `attached`
        case `cols`
        case `rows`
        case `next_offset`
        case `grid_revision`
        case `stale`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`terminal_id` = try c.decode(String.self, forKey: .`terminal_id`)
        if !c.contains(.`workspace_id`) { self.`workspace_id` = "" } else {
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        }
        if !c.contains(.`label`) { self.`label` = "Terminal" } else {
        self.`label` = try c.decode(String.self, forKey: .`label`)
        }
        if !c.contains(.`session_status`) { self.`session_status` = "running" } else {
        self.`session_status` = try c.decode(String.self, forKey: .`session_status`)
        }
        if !c.contains(.`attached`) { self.`attached` = false } else {
        self.`attached` = try c.decode(Bool.self, forKey: .`attached`)
        }
        if !c.contains(.`cols`) { self.`cols` = 80 } else {
        self.`cols` = try c.decode(UInt16.self, forKey: .`cols`)
        }
        if !c.contains(.`rows`) { self.`rows` = 24 } else {
        self.`rows` = try c.decode(UInt16.self, forKey: .`rows`)
        }
        if !c.contains(.`next_offset`) { self.`next_offset` = nil } else {
        self.`next_offset` = try c.decodeIfPresent(String.self, forKey: .`next_offset`)
        }
        if !c.contains(.`grid_revision`) { self.`grid_revision` = "0" } else {
        self.`grid_revision` = try c.decode(String.self, forKey: .`grid_revision`)
        }
        if !c.contains(.`stale`) { self.`stale` = true } else {
        self.`stale` = try c.decode(Bool.self, forKey: .`stale`)
        }
        if !c.contains(.`error`) { self.`error` = nil } else {
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`label`, forKey: .`label`)
        try c.encode(self.`session_status`, forKey: .`session_status`)
        try c.encode(self.`attached`, forKey: .`attached`)
        try c.encode(self.`cols`, forKey: .`cols`)
        try c.encode(self.`rows`, forKey: .`rows`)
        try c.encode(self.`next_offset`, forKey: .`next_offset`)
        try c.encode(self.`grid_revision`, forKey: .`grid_revision`)
        try c.encode(self.`stale`, forKey: .`stale`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct TerminalQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: TerminalView?
    var `error`: LocalError?
}

extension TerminalQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(TerminalView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ChatAttachment: Codable {
    var `local_id`: String
    var `name`: String
    var `mime`: String
    var `byte_size`: String
    var `attachment_id`: String? = nil
    var `reference`: String? = nil
    var `uploaded_bytes`: String = "0"
    var `status`: String = "local"
    var `error`: LocalError? = nil
}

extension ChatAttachment {
    private enum CodingKeys: String, CodingKey {
        case `local_id`
        case `name`
        case `mime`
        case `byte_size`
        case `attachment_id`
        case `reference`
        case `uploaded_bytes`
        case `status`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`local_id` = try c.decode(String.self, forKey: .`local_id`)
        self.`name` = try c.decode(String.self, forKey: .`name`)
        self.`mime` = try c.decode(String.self, forKey: .`mime`)
        self.`byte_size` = try c.decode(String.self, forKey: .`byte_size`)
        if !c.contains(.`attachment_id`) { self.`attachment_id` = nil } else {
        self.`attachment_id` = try c.decodeIfPresent(String.self, forKey: .`attachment_id`)
        }
        if !c.contains(.`reference`) { self.`reference` = nil } else {
        self.`reference` = try c.decodeIfPresent(String.self, forKey: .`reference`)
        }
        if !c.contains(.`uploaded_bytes`) { self.`uploaded_bytes` = "0" } else {
        self.`uploaded_bytes` = try c.decode(String.self, forKey: .`uploaded_bytes`)
        }
        if !c.contains(.`status`) { self.`status` = "local" } else {
        self.`status` = try c.decode(String.self, forKey: .`status`)
        }
        if !c.contains(.`error`) { self.`error` = nil } else {
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`local_id`, forKey: .`local_id`)
        try c.encode(self.`name`, forKey: .`name`)
        try c.encode(self.`mime`, forKey: .`mime`)
        try c.encode(self.`byte_size`, forKey: .`byte_size`)
        try c.encode(self.`attachment_id`, forKey: .`attachment_id`)
        try c.encode(self.`reference`, forKey: .`reference`)
        try c.encode(self.`uploaded_bytes`, forKey: .`uploaded_bytes`)
        try c.encode(self.`status`, forKey: .`status`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ChatTool: Codable {
    var `id`: String
    var `kind`: String
    var `status`: String
}

extension ChatTool {
    private enum CodingKeys: String, CodingKey {
        case `id`
        case `kind`
        case `status`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`id` = try c.decode(String.self, forKey: .`id`)
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
        self.`status` = try c.decode(String.self, forKey: .`status`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`id`, forKey: .`id`)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`status`, forKey: .`status`)
    }
}

struct ChatRow: Codable {
    var `id`: String
    var `role`: String
    var `kind`: String = "message"
    var `author`: String = ""
    var `body`: String
    var `created_at_ms`: Int64? = nil
    var `delivery`: String = "committed"
    var `attachments`: [ChatAttachment] = []
    var `tool`: ChatTool? = nil
}

extension ChatRow {
    private enum CodingKeys: String, CodingKey {
        case `id`
        case `role`
        case `kind`
        case `author`
        case `body`
        case `created_at_ms`
        case `delivery`
        case `attachments`
        case `tool`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`id` = try c.decode(String.self, forKey: .`id`)
        self.`role` = try c.decode(String.self, forKey: .`role`)
        if !c.contains(.`kind`) { self.`kind` = "message" } else {
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
        }
        if !c.contains(.`author`) { self.`author` = "" } else {
        self.`author` = try c.decode(String.self, forKey: .`author`)
        }
        self.`body` = try c.decode(String.self, forKey: .`body`)
        if !c.contains(.`created_at_ms`) { self.`created_at_ms` = nil } else {
        self.`created_at_ms` = try c.decodeIfPresent(Int64.self, forKey: .`created_at_ms`)
        }
        if !c.contains(.`delivery`) { self.`delivery` = "committed" } else {
        self.`delivery` = try c.decode(String.self, forKey: .`delivery`)
        }
        if !c.contains(.`attachments`) { self.`attachments` = [] } else {
        self.`attachments` = try c.decode([ChatAttachment].self, forKey: .`attachments`)
        }
        if !c.contains(.`tool`) { self.`tool` = nil } else {
        self.`tool` = try c.decodeIfPresent(ChatTool.self, forKey: .`tool`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`id`, forKey: .`id`)
        try c.encode(self.`role`, forKey: .`role`)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`author`, forKey: .`author`)
        try c.encode(self.`body`, forKey: .`body`)
        try c.encode(self.`created_at_ms`, forKey: .`created_at_ms`)
        try c.encode(self.`delivery`, forKey: .`delivery`)
        try c.encode(self.`attachments`, forKey: .`attachments`)
        try c.encode(self.`tool`, forKey: .`tool`)
    }
}

struct ChatTurn: Codable {
    var `turn_id`: String
    var `status`: String
    var `after_seq`: String = "0"
    var `started_at_ms`: Int64? = nil
    var `elapsed_ms`: Int64 = 0
    var `stop_pending`: Bool = false
}

extension ChatTurn {
    private enum CodingKeys: String, CodingKey {
        case `turn_id`
        case `status`
        case `after_seq`
        case `started_at_ms`
        case `elapsed_ms`
        case `stop_pending`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`turn_id` = try c.decode(String.self, forKey: .`turn_id`)
        self.`status` = try c.decode(String.self, forKey: .`status`)
        if !c.contains(.`after_seq`) { self.`after_seq` = "0" } else {
        self.`after_seq` = try c.decode(String.self, forKey: .`after_seq`)
        }
        if !c.contains(.`started_at_ms`) { self.`started_at_ms` = nil } else {
        self.`started_at_ms` = try c.decodeIfPresent(Int64.self, forKey: .`started_at_ms`)
        }
        if !c.contains(.`elapsed_ms`) { self.`elapsed_ms` = 0 } else {
        self.`elapsed_ms` = try c.decode(Int64.self, forKey: .`elapsed_ms`)
        }
        if !c.contains(.`stop_pending`) { self.`stop_pending` = false } else {
        self.`stop_pending` = try c.decode(Bool.self, forKey: .`stop_pending`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`turn_id`, forKey: .`turn_id`)
        try c.encode(self.`status`, forKey: .`status`)
        try c.encode(self.`after_seq`, forKey: .`after_seq`)
        try c.encode(self.`started_at_ms`, forKey: .`started_at_ms`)
        try c.encode(self.`elapsed_ms`, forKey: .`elapsed_ms`)
        try c.encode(self.`stop_pending`, forKey: .`stop_pending`)
    }
}

struct ChatApproval: Codable {
    var `turn_id`: String
    var `call_id`: String
    var `title`: String
    var `body`: String
    var `resolution`: String = "idle"
    var `error`: LocalError? = nil
}

extension ChatApproval {
    private enum CodingKeys: String, CodingKey {
        case `turn_id`
        case `call_id`
        case `title`
        case `body`
        case `resolution`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`turn_id` = try c.decode(String.self, forKey: .`turn_id`)
        self.`call_id` = try c.decode(String.self, forKey: .`call_id`)
        self.`title` = try c.decode(String.self, forKey: .`title`)
        self.`body` = try c.decode(String.self, forKey: .`body`)
        if !c.contains(.`resolution`) { self.`resolution` = "idle" } else {
        self.`resolution` = try c.decode(String.self, forKey: .`resolution`)
        }
        if !c.contains(.`error`) { self.`error` = nil } else {
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`turn_id`, forKey: .`turn_id`)
        try c.encode(self.`call_id`, forKey: .`call_id`)
        try c.encode(self.`title`, forKey: .`title`)
        try c.encode(self.`body`, forKey: .`body`)
        try c.encode(self.`resolution`, forKey: .`resolution`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ChatFollowup: Codable {
    var `id`: String
    var `kind`: String
    var `state`: String = "pending"
    var `delivery`: String = "unsent"
    var `turn_id`: String
    var `steer_id`: String
    var `next_turn_id`: String
    var `text`: String
    var `attachments`: [ChatAttachment] = []
    var `paused`: Bool = false
    var `can_retry`: Bool = false
    var `can_pull_back`: Bool = true
    var `error`: LocalError? = nil
}

extension ChatFollowup {
    private enum CodingKeys: String, CodingKey {
        case `id`
        case `kind`
        case `state`
        case `delivery`
        case `turn_id`
        case `steer_id`
        case `next_turn_id`
        case `text`
        case `attachments`
        case `paused`
        case `can_retry`
        case `can_pull_back`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`id` = try c.decode(String.self, forKey: .`id`)
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
        if !c.contains(.`state`) { self.`state` = "pending" } else {
        self.`state` = try c.decode(String.self, forKey: .`state`)
        }
        if !c.contains(.`delivery`) { self.`delivery` = "unsent" } else {
        self.`delivery` = try c.decode(String.self, forKey: .`delivery`)
        }
        self.`turn_id` = try c.decode(String.self, forKey: .`turn_id`)
        self.`steer_id` = try c.decode(String.self, forKey: .`steer_id`)
        self.`next_turn_id` = try c.decode(String.self, forKey: .`next_turn_id`)
        self.`text` = try c.decode(String.self, forKey: .`text`)
        if !c.contains(.`attachments`) { self.`attachments` = [] } else {
        self.`attachments` = try c.decode([ChatAttachment].self, forKey: .`attachments`)
        }
        if !c.contains(.`paused`) { self.`paused` = false } else {
        self.`paused` = try c.decode(Bool.self, forKey: .`paused`)
        }
        if !c.contains(.`can_retry`) { self.`can_retry` = false } else {
        self.`can_retry` = try c.decode(Bool.self, forKey: .`can_retry`)
        }
        if !c.contains(.`can_pull_back`) { self.`can_pull_back` = true } else {
        self.`can_pull_back` = try c.decode(Bool.self, forKey: .`can_pull_back`)
        }
        if !c.contains(.`error`) { self.`error` = nil } else {
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`id`, forKey: .`id`)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`state`, forKey: .`state`)
        try c.encode(self.`delivery`, forKey: .`delivery`)
        try c.encode(self.`turn_id`, forKey: .`turn_id`)
        try c.encode(self.`steer_id`, forKey: .`steer_id`)
        try c.encode(self.`next_turn_id`, forKey: .`next_turn_id`)
        try c.encode(self.`text`, forKey: .`text`)
        try c.encode(self.`attachments`, forKey: .`attachments`)
        try c.encode(self.`paused`, forKey: .`paused`)
        try c.encode(self.`can_retry`, forKey: .`can_retry`)
        try c.encode(self.`can_pull_back`, forKey: .`can_pull_back`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ChatSelection: Codable {
    var `provider`: String? = nil
    var `model`: String? = nil
    var `effort`: String? = nil
    var `access`: String? = nil
    var `speed`: String? = nil
}

extension ChatSelection {
    private enum CodingKeys: String, CodingKey {
        case `provider`
        case `model`
        case `effort`
        case `access`
        case `speed`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`provider`) { self.`provider` = nil } else {
        self.`provider` = try c.decodeIfPresent(String.self, forKey: .`provider`)
        }
        if !c.contains(.`model`) { self.`model` = nil } else {
        self.`model` = try c.decodeIfPresent(String.self, forKey: .`model`)
        }
        if !c.contains(.`effort`) { self.`effort` = nil } else {
        self.`effort` = try c.decodeIfPresent(String.self, forKey: .`effort`)
        }
        if !c.contains(.`access`) { self.`access` = nil } else {
        self.`access` = try c.decodeIfPresent(String.self, forKey: .`access`)
        }
        if !c.contains(.`speed`) { self.`speed` = nil } else {
        self.`speed` = try c.decodeIfPresent(String.self, forKey: .`speed`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`provider`, forKey: .`provider`)
        try c.encode(self.`model`, forKey: .`model`)
        try c.encode(self.`effort`, forKey: .`effort`)
        try c.encode(self.`access`, forKey: .`access`)
        try c.encode(self.`speed`, forKey: .`speed`)
    }
}

struct ChatChoice: Codable {
    var `id`: String
    var `label`: String
    var `enabled`: Bool = true
    var `reason`: String? = nil
    var `favorite`: Bool = false
}

extension ChatChoice {
    private enum CodingKeys: String, CodingKey {
        case `id`
        case `label`
        case `enabled`
        case `reason`
        case `favorite`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`id` = try c.decode(String.self, forKey: .`id`)
        self.`label` = try c.decode(String.self, forKey: .`label`)
        if !c.contains(.`enabled`) { self.`enabled` = true } else {
        self.`enabled` = try c.decode(Bool.self, forKey: .`enabled`)
        }
        if !c.contains(.`reason`) { self.`reason` = nil } else {
        self.`reason` = try c.decodeIfPresent(String.self, forKey: .`reason`)
        }
        if !c.contains(.`favorite`) { self.`favorite` = false } else {
        self.`favorite` = try c.decode(Bool.self, forKey: .`favorite`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`id`, forKey: .`id`)
        try c.encode(self.`label`, forKey: .`label`)
        try c.encode(self.`enabled`, forKey: .`enabled`)
        try c.encode(self.`reason`, forKey: .`reason`)
        try c.encode(self.`favorite`, forKey: .`favorite`)
    }
}

struct ChatCatalogs: Codable {
    var `models`: [ChatChoice] = []
    var `efforts`: [ChatChoice] = []
    var `access`: [ChatChoice] = []
    var `speeds`: [ChatChoice] = []
    var `slash`: [ChatChoice] = []
}

extension ChatCatalogs {
    private enum CodingKeys: String, CodingKey {
        case `models`
        case `efforts`
        case `access`
        case `speeds`
        case `slash`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`models`) { self.`models` = [] } else {
        self.`models` = try c.decode([ChatChoice].self, forKey: .`models`)
        }
        if !c.contains(.`efforts`) { self.`efforts` = [] } else {
        self.`efforts` = try c.decode([ChatChoice].self, forKey: .`efforts`)
        }
        if !c.contains(.`access`) { self.`access` = [] } else {
        self.`access` = try c.decode([ChatChoice].self, forKey: .`access`)
        }
        if !c.contains(.`speeds`) { self.`speeds` = [] } else {
        self.`speeds` = try c.decode([ChatChoice].self, forKey: .`speeds`)
        }
        if !c.contains(.`slash`) { self.`slash` = [] } else {
        self.`slash` = try c.decode([ChatChoice].self, forKey: .`slash`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`models`, forKey: .`models`)
        try c.encode(self.`efforts`, forKey: .`efforts`)
        try c.encode(self.`access`, forKey: .`access`)
        try c.encode(self.`speeds`, forKey: .`speeds`)
        try c.encode(self.`slash`, forKey: .`slash`)
    }
}

struct ChatDraft: Codable {
    var `revision`: String
    var `text`: String
    var `attachments`: [ChatAttachment]
    var `persisted`: Bool
}

extension ChatDraft {
    private enum CodingKeys: String, CodingKey {
        case `revision`
        case `text`
        case `attachments`
        case `persisted`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`text` = try c.decode(String.self, forKey: .`text`)
        self.`attachments` = try c.decode([ChatAttachment].self, forKey: .`attachments`)
        self.`persisted` = try c.decode(Bool.self, forKey: .`persisted`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`text`, forKey: .`text`)
        try c.encode(self.`attachments`, forKey: .`attachments`)
        try c.encode(self.`persisted`, forKey: .`persisted`)
    }
}

struct ChatMention: Codable {
    var `path`: String
    var `label`: String
}

extension ChatMention {
    private enum CodingKeys: String, CodingKey {
        case `path`
        case `label`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`path` = try c.decode(String.self, forKey: .`path`)
        self.`label` = try c.decode(String.self, forKey: .`label`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`path`, forKey: .`path`)
        try c.encode(self.`label`, forKey: .`label`)
    }
}

struct ChatShellConfirmation: Codable {
    var `id`: String
    var `command`: String
    var `cwd`: String
}

extension ChatShellConfirmation {
    private enum CodingKeys: String, CodingKey {
        case `id`
        case `command`
        case `cwd`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`id` = try c.decode(String.self, forKey: .`id`)
        self.`command` = try c.decode(String.self, forKey: .`command`)
        self.`cwd` = try c.decode(String.self, forKey: .`cwd`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`id`, forKey: .`id`)
        try c.encode(self.`command`, forKey: .`command`)
        try c.encode(self.`cwd`, forKey: .`cwd`)
    }
}

struct ChatUsageLimit: Codable {
    var `label`: String
    var `percent_left`: UInt8
    var `reset`: String
}

extension ChatUsageLimit {
    private enum CodingKeys: String, CodingKey {
        case `label`
        case `percent_left`
        case `reset`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`label` = try c.decode(String.self, forKey: .`label`)
        self.`percent_left` = try c.decode(UInt8.self, forKey: .`percent_left`)
        self.`reset` = try c.decode(String.self, forKey: .`reset`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`label`, forKey: .`label`)
        try c.encode(self.`percent_left`, forKey: .`percent_left`)
        try c.encode(self.`reset`, forKey: .`reset`)
    }
}

struct ChatUsageStat: Codable {
    var `label`: String
    var `value`: String
}

extension ChatUsageStat {
    private enum CodingKeys: String, CodingKey {
        case `label`
        case `value`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`label` = try c.decode(String.self, forKey: .`label`)
        self.`value` = try c.decode(String.self, forKey: .`value`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`label`, forKey: .`label`)
        try c.encode(self.`value`, forKey: .`value`)
    }
}

struct ChatUsage: Codable {
    var `provider`: String
    var `limits`: [ChatUsageLimit] = []
    var `stats`: [ChatUsageStat] = []
    var `recent`: [ChatUsageStat] = []
}

extension ChatUsage {
    private enum CodingKeys: String, CodingKey {
        case `provider`
        case `limits`
        case `stats`
        case `recent`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`provider` = try c.decode(String.self, forKey: .`provider`)
        if !c.contains(.`limits`) { self.`limits` = [] } else {
        self.`limits` = try c.decode([ChatUsageLimit].self, forKey: .`limits`)
        }
        if !c.contains(.`stats`) { self.`stats` = [] } else {
        self.`stats` = try c.decode([ChatUsageStat].self, forKey: .`stats`)
        }
        if !c.contains(.`recent`) { self.`recent` = [] } else {
        self.`recent` = try c.decode([ChatUsageStat].self, forKey: .`recent`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`provider`, forKey: .`provider`)
        try c.encode(self.`limits`, forKey: .`limits`)
        try c.encode(self.`stats`, forKey: .`stats`)
        try c.encode(self.`recent`, forKey: .`recent`)
    }
}

struct ChatPage: Codable {
    var `has_older`: Bool
    var `cursor`: String?
    var `loading`: Bool
}

extension ChatPage {
    private enum CodingKeys: String, CodingKey {
        case `has_older`
        case `cursor`
        case `loading`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`has_older` = try c.decode(Bool.self, forKey: .`has_older`)
        self.`cursor` = try c.decodeIfPresent(String.self, forKey: .`cursor`)
        self.`loading` = try c.decode(Bool.self, forKey: .`loading`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`has_older`, forKey: .`has_older`)
        try c.encode(self.`cursor`, forKey: .`cursor`)
        try c.encode(self.`loading`, forKey: .`loading`)
    }
}

struct ChatThreadView: Codable {
    var `thread`: ThreadSummary
    var `rows`: [ChatRow]
    var `page`: ChatPage
    var `turn`: ChatTurn?
    var `approval`: ChatApproval?
    var `usage`: ChatUsage?
    var `stale`: Bool
    var `error`: LocalError?
}

extension ChatThreadView {
    private enum CodingKeys: String, CodingKey {
        case `thread`
        case `rows`
        case `page`
        case `turn`
        case `approval`
        case `usage`
        case `stale`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`thread` = try c.decode(ThreadSummary.self, forKey: .`thread`)
        self.`rows` = try c.decode([ChatRow].self, forKey: .`rows`)
        self.`page` = try c.decode(ChatPage.self, forKey: .`page`)
        self.`turn` = try c.decodeIfPresent(ChatTurn.self, forKey: .`turn`)
        self.`approval` = try c.decodeIfPresent(ChatApproval.self, forKey: .`approval`)
        self.`usage` = try c.decodeIfPresent(ChatUsage.self, forKey: .`usage`)
        self.`stale` = try c.decode(Bool.self, forKey: .`stale`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`thread`, forKey: .`thread`)
        try c.encode(self.`rows`, forKey: .`rows`)
        try c.encode(self.`page`, forKey: .`page`)
        try c.encode(self.`turn`, forKey: .`turn`)
        try c.encode(self.`approval`, forKey: .`approval`)
        try c.encode(self.`usage`, forKey: .`usage`)
        try c.encode(self.`stale`, forKey: .`stale`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ChatComposerView: Codable {
    var `draft`: ChatDraft
    var `selection`: ChatSelection
    var `catalogs`: ChatCatalogs
    var `mentions`: [ChatMention]
    var `provider_ready`: Bool
    var `can_send`: Bool
    var `can_stop`: Bool
    var `send_operation`: Operation?
    var `followup`: ChatFollowup?
    var `shell_confirmation`: ChatShellConfirmation?
    var `error`: LocalError?
}

extension ChatComposerView {
    private enum CodingKeys: String, CodingKey {
        case `draft`
        case `selection`
        case `catalogs`
        case `mentions`
        case `provider_ready`
        case `can_send`
        case `can_stop`
        case `send_operation`
        case `followup`
        case `shell_confirmation`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`draft` = try c.decode(ChatDraft.self, forKey: .`draft`)
        self.`selection` = try c.decode(ChatSelection.self, forKey: .`selection`)
        self.`catalogs` = try c.decode(ChatCatalogs.self, forKey: .`catalogs`)
        self.`mentions` = try c.decode([ChatMention].self, forKey: .`mentions`)
        self.`provider_ready` = try c.decode(Bool.self, forKey: .`provider_ready`)
        self.`can_send` = try c.decode(Bool.self, forKey: .`can_send`)
        self.`can_stop` = try c.decode(Bool.self, forKey: .`can_stop`)
        self.`send_operation` = try c.decodeIfPresent(Operation.self, forKey: .`send_operation`)
        self.`followup` = try c.decodeIfPresent(ChatFollowup.self, forKey: .`followup`)
        self.`shell_confirmation` = try c.decodeIfPresent(ChatShellConfirmation.self, forKey: .`shell_confirmation`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`draft`, forKey: .`draft`)
        try c.encode(self.`selection`, forKey: .`selection`)
        try c.encode(self.`catalogs`, forKey: .`catalogs`)
        try c.encode(self.`mentions`, forKey: .`mentions`)
        try c.encode(self.`provider_ready`, forKey: .`provider_ready`)
        try c.encode(self.`can_send`, forKey: .`can_send`)
        try c.encode(self.`can_stop`, forKey: .`can_stop`)
        try c.encode(self.`send_operation`, forKey: .`send_operation`)
        try c.encode(self.`followup`, forKey: .`followup`)
        try c.encode(self.`shell_confirmation`, forKey: .`shell_confirmation`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ThreadQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: ChatThreadView?
    var `error`: LocalError?
}

extension ThreadQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(ChatThreadView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ComposerQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: ChatComposerView?
    var `error`: LocalError?
}

extension ComposerQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(ChatComposerView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct Config: Codable {
    var `api_version`: UInt32
    var `host_id`: String
    var `label`: String
    var `https_url`: String?
    var `wss_url`: String?
    var `client_revision`: UInt32
    var `session_nonce`: String
    var `jitter_seed`: UInt64
}

extension Config {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `host_id`
        case `label`
        case `https_url`
        case `wss_url`
        case `client_revision`
        case `session_nonce`
        case `jitter_seed`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`host_id` = try c.decode(String.self, forKey: .`host_id`)
        self.`label` = try c.decode(String.self, forKey: .`label`)
        self.`https_url` = try c.decodeIfPresent(String.self, forKey: .`https_url`)
        self.`wss_url` = try c.decodeIfPresent(String.self, forKey: .`wss_url`)
        self.`client_revision` = try c.decode(UInt32.self, forKey: .`client_revision`)
        self.`session_nonce` = try c.decode(String.self, forKey: .`session_nonce`)
        self.`jitter_seed` = try c.decode(UInt64.self, forKey: .`jitter_seed`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`host_id`, forKey: .`host_id`)
        try c.encode(self.`label`, forKey: .`label`)
        try c.encode(self.`https_url`, forKey: .`https_url`)
        try c.encode(self.`wss_url`, forKey: .`wss_url`)
        try c.encode(self.`client_revision`, forKey: .`client_revision`)
        try c.encode(self.`session_nonce`, forKey: .`session_nonce`)
        try c.encode(self.`jitter_seed`, forKey: .`jitter_seed`)
    }
}

enum Lifecycle: String, Codable {
    case `created`
    case `foreground`
    case `background`
    case `stopped`
}

struct LocalError: Codable {
    var `domain`: String = "input"
    var `code`: String
    var `message`: String
    var `failure_kind`: String? = nil
    var `retryable`: Bool = false
    var `retry_after_ms`: UInt32? = nil
    var `intent_id`: String? = nil
    var `rpc_code`: String? = nil
    var `delivery`: String? = "rejected"
}

extension LocalError {
    private enum CodingKeys: String, CodingKey {
        case `domain`
        case `code`
        case `message`
        case `failure_kind`
        case `retryable`
        case `retry_after_ms`
        case `intent_id`
        case `rpc_code`
        case `delivery`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`domain`) { self.`domain` = "input" } else {
        self.`domain` = try c.decode(String.self, forKey: .`domain`)
        }
        self.`code` = try c.decode(String.self, forKey: .`code`)
        self.`message` = try c.decode(String.self, forKey: .`message`)
        if !c.contains(.`failure_kind`) { self.`failure_kind` = nil } else {
        self.`failure_kind` = try c.decodeIfPresent(String.self, forKey: .`failure_kind`)
        }
        if !c.contains(.`retryable`) { self.`retryable` = false } else {
        self.`retryable` = try c.decode(Bool.self, forKey: .`retryable`)
        }
        if !c.contains(.`retry_after_ms`) { self.`retry_after_ms` = nil } else {
        self.`retry_after_ms` = try c.decodeIfPresent(UInt32.self, forKey: .`retry_after_ms`)
        }
        if !c.contains(.`intent_id`) { self.`intent_id` = nil } else {
        self.`intent_id` = try c.decodeIfPresent(String.self, forKey: .`intent_id`)
        }
        if !c.contains(.`rpc_code`) { self.`rpc_code` = nil } else {
        self.`rpc_code` = try c.decodeIfPresent(String.self, forKey: .`rpc_code`)
        }
        if !c.contains(.`delivery`) { self.`delivery` = "rejected" } else {
        self.`delivery` = try c.decodeIfPresent(String.self, forKey: .`delivery`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`domain`, forKey: .`domain`)
        try c.encode(self.`code`, forKey: .`code`)
        try c.encode(self.`message`, forKey: .`message`)
        try c.encode(self.`failure_kind`, forKey: .`failure_kind`)
        try c.encode(self.`retryable`, forKey: .`retryable`)
        try c.encode(self.`retry_after_ms`, forKey: .`retry_after_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`rpc_code`, forKey: .`rpc_code`)
        try c.encode(self.`delivery`, forKey: .`delivery`)
    }
}

struct Operation: Codable {
    var `intent_id`: String
    var `state`: String = "failed"
    var `error`: LocalError?
}

extension Operation {
    private enum CodingKeys: String, CodingKey {
        case `intent_id`
        case `state`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        if !c.contains(.`state`) { self.`state` = "failed" } else {
        self.`state` = try c.decode(String.self, forKey: .`state`)
        }
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`state`, forKey: .`state`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

enum TransportFailureKind: String, Codable {
    case `network`
    case `timeout`
    case `cancelled`
    case `tls`
    case `server_unavailable`
    case `resource`
}

enum TransportFailureCode: String, Codable {
    case `unknown`
    case `offline`
    case `dns`
    case `refused`
    case `reset`
    case `timeout`
    case `cancelled`
    case `certificate`
    case `hostname`
    case `pin_mismatch`
    case `unavailable`
    case `resource`
}

struct TransportFailure: Codable {
    var `kind`: TransportFailureKind
    var `code`: TransportFailureCode
}

extension TransportFailure {
    private enum CodingKeys: String, CodingKey {
        case `kind`
        case `code`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`kind` = try c.decode(TransportFailureKind.self, forKey: .`kind`)
        self.`code` = try c.decode(TransportFailureCode.self, forKey: .`code`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`code`, forKey: .`code`)
    }
}

enum PlatformFailureCode: String, Codable {
    case `unavailable`
    case `locked`
    case `denied`
    case `io`
    case `resource`
}

struct PlatformFailure: Codable {
    var `code`: PlatformFailureCode
}

extension PlatformFailure {
    private enum CodingKeys: String, CodingKey {
        case `code`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`code` = try c.decode(PlatformFailureCode.self, forKey: .`code`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`code`, forKey: .`code`)
    }
}

struct Header: Codable {
    var `name`: String
    var `value`: String
}

extension Header {
    private enum CodingKeys: String, CodingKey {
        case `name`
        case `value`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`name` = try c.decode(String.self, forKey: .`name`)
        self.`value` = try c.decode(String.self, forKey: .`value`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`name`, forKey: .`name`)
        try c.encode(self.`value`, forKey: .`value`)
    }
}

struct Tls: Codable {
    var `origin`: String
    var `spki_sha256`: String
}

extension Tls {
    private enum CodingKeys: String, CodingKey {
        case `origin`
        case `spki_sha256`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`origin` = try c.decode(String.self, forKey: .`origin`)
        self.`spki_sha256` = try c.decode(String.self, forKey: .`spki_sha256`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`origin`, forKey: .`origin`)
        try c.encode(self.`spki_sha256`, forKey: .`spki_sha256`)
    }
}

struct VtModes: Codable {
    var `application_cursor`: Bool
    var `bracketed_paste`: Bool
}

extension VtModes {
    private enum CodingKeys: String, CodingKey {
        case `application_cursor`
        case `bracketed_paste`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`application_cursor` = try c.decode(Bool.self, forKey: .`application_cursor`)
        self.`bracketed_paste` = try c.decode(Bool.self, forKey: .`bracketed_paste`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`application_cursor`, forKey: .`application_cursor`)
        try c.encode(self.`bracketed_paste`, forKey: .`bracketed_paste`)
    }
}

struct AttachmentInput: Codable {
    var `local_id`: String
    var `name`: String
    var `mime`: String
    var `byte_size`: String
    var `bytes_base64`: String
}

extension AttachmentInput {
    private enum CodingKeys: String, CodingKey {
        case `local_id`
        case `name`
        case `mime`
        case `byte_size`
        case `bytes_base64`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`local_id` = try c.decode(String.self, forKey: .`local_id`)
        self.`name` = try c.decode(String.self, forKey: .`name`)
        self.`mime` = try c.decode(String.self, forKey: .`mime`)
        self.`byte_size` = try c.decode(String.self, forKey: .`byte_size`)
        self.`bytes_base64` = try c.decode(String.self, forKey: .`bytes_base64`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`local_id`, forKey: .`local_id`)
        try c.encode(self.`name`, forKey: .`name`)
        try c.encode(self.`mime`, forKey: .`mime`)
        try c.encode(self.`byte_size`, forKey: .`byte_size`)
        try c.encode(self.`bytes_base64`, forKey: .`bytes_base64`)
    }
}

enum Event: Codable {
    case `sign_out`(EventSignOut)
    case `forget_host`(EventForgetHost)
    case `start`(EventStart)
    case `foreground`(EventForeground)
    case `background`(EventBackground)
    case `shutdown`(EventShutdown)
    case `network_changed`(EventNetworkChanged)
    case `http_response`(EventHttpResponse)
    case `ws_open`(EventWsOpen)
    case `ws_message`(EventWsMessage)
    case `ws_closed`(EventWsClosed)
    case `timer_fired`(EventTimerFired)
    case `secure_store_value`(EventSecureStoreValue)
    case `secure_store_done`(EventSecureStoreDone)
    case `tls_peer`(EventTlsPeer)
    case `terminal_applied`(EventTerminalApplied)
    case `terminal_reply`(EventTerminalReply)
    case `pair`(EventPair)
    case `trust_decision`(EventTrustDecision)
    case `retry_connection`(EventRetryConnection)
    case `focus`(EventFocus)
    case `history_search`(EventHistorySearch)
    case `history_load_more`(EventHistoryLoadMore)
    case `shell_confirm`(EventShellConfirm)
    case `terminal_create`(EventTerminalCreate)
    case `thread_open`(EventThreadOpen)
    case `thread_load_older`(EventThreadLoadOlder)
    case `draft_set`(EventDraftSet)
    case `composer_select`(EventComposerSelect)
    case `send`(EventSend)
    case `turn_cancel`(EventTurnCancel)
    case `followup_submit`(EventFollowupSubmit)
    case `followup_retry`(EventFollowupRetry)
    case `followup_pull_back`(EventFollowupPullBack)
    case `followup_cancel`(EventFollowupCancel)
    case `approval_decide`(EventApprovalDecide)
    case `shell_prepare`(EventShellPrepare)
    case `slash_search`(EventSlashSearch)
    case `slash_run`(EventSlashRun)
    case `mention_search`(EventMentionSearch)
    case `terminal_attach`(EventTerminalAttach)
    case `terminal_detach`(EventTerminalDetach)
    case `terminal_kill`(EventTerminalKill)
    case `terminal_resize`(EventTerminalResize)
    case `push_register`(EventPushRegister)
    case `thread_rename`(EventThreadRename)
    case `thread_close`(EventThreadClose)
    case `thread_sync`(EventThreadSync)
    case `thread_create`(EventThreadCreate)
    case `new_chat_select`(EventNewChatSelect)
    case `workspace_create`(EventWorkspaceCreate)
    case `workspace_rename`(EventWorkspaceRename)
    case `workspace_archive`(EventWorkspaceArchive)
    case `workspace_close`(EventWorkspaceClose)
    case `directory_list`(EventDirectoryList)
    case `file_open`(EventFileOpen)
    case `push_received`(EventPushReceived)
    case `terminal_input`(EventTerminalInput)
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: ModelDiscriminator.self)
        let tag = try c.decode(String.self, forKey: .type)
        switch tag {
        case "sign_out": self = .`sign_out`(try EventSignOut(from: decoder))
        case "forget_host": self = .`forget_host`(try EventForgetHost(from: decoder))
        case "start": self = .`start`(try EventStart(from: decoder))
        case "foreground": self = .`foreground`(try EventForeground(from: decoder))
        case "background": self = .`background`(try EventBackground(from: decoder))
        case "shutdown": self = .`shutdown`(try EventShutdown(from: decoder))
        case "network_changed": self = .`network_changed`(try EventNetworkChanged(from: decoder))
        case "http_response": self = .`http_response`(try EventHttpResponse(from: decoder))
        case "ws_open": self = .`ws_open`(try EventWsOpen(from: decoder))
        case "ws_message": self = .`ws_message`(try EventWsMessage(from: decoder))
        case "ws_closed": self = .`ws_closed`(try EventWsClosed(from: decoder))
        case "timer_fired": self = .`timer_fired`(try EventTimerFired(from: decoder))
        case "secure_store_value": self = .`secure_store_value`(try EventSecureStoreValue(from: decoder))
        case "secure_store_done": self = .`secure_store_done`(try EventSecureStoreDone(from: decoder))
        case "tls_peer": self = .`tls_peer`(try EventTlsPeer(from: decoder))
        case "terminal_applied": self = .`terminal_applied`(try EventTerminalApplied(from: decoder))
        case "terminal_reply": self = .`terminal_reply`(try EventTerminalReply(from: decoder))
        case "pair": self = .`pair`(try EventPair(from: decoder))
        case "trust_decision": self = .`trust_decision`(try EventTrustDecision(from: decoder))
        case "retry_connection": self = .`retry_connection`(try EventRetryConnection(from: decoder))
        case "focus": self = .`focus`(try EventFocus(from: decoder))
        case "history_search": self = .`history_search`(try EventHistorySearch(from: decoder))
        case "history_load_more": self = .`history_load_more`(try EventHistoryLoadMore(from: decoder))
        case "shell_confirm": self = .`shell_confirm`(try EventShellConfirm(from: decoder))
        case "terminal_create": self = .`terminal_create`(try EventTerminalCreate(from: decoder))
        case "thread_open": self = .`thread_open`(try EventThreadOpen(from: decoder))
        case "thread_load_older": self = .`thread_load_older`(try EventThreadLoadOlder(from: decoder))
        case "draft_set": self = .`draft_set`(try EventDraftSet(from: decoder))
        case "composer_select": self = .`composer_select`(try EventComposerSelect(from: decoder))
        case "send": self = .`send`(try EventSend(from: decoder))
        case "turn_cancel": self = .`turn_cancel`(try EventTurnCancel(from: decoder))
        case "followup_submit": self = .`followup_submit`(try EventFollowupSubmit(from: decoder))
        case "followup_retry": self = .`followup_retry`(try EventFollowupRetry(from: decoder))
        case "followup_pull_back": self = .`followup_pull_back`(try EventFollowupPullBack(from: decoder))
        case "followup_cancel": self = .`followup_cancel`(try EventFollowupCancel(from: decoder))
        case "approval_decide": self = .`approval_decide`(try EventApprovalDecide(from: decoder))
        case "shell_prepare": self = .`shell_prepare`(try EventShellPrepare(from: decoder))
        case "slash_search": self = .`slash_search`(try EventSlashSearch(from: decoder))
        case "slash_run": self = .`slash_run`(try EventSlashRun(from: decoder))
        case "mention_search": self = .`mention_search`(try EventMentionSearch(from: decoder))
        case "terminal_attach": self = .`terminal_attach`(try EventTerminalAttach(from: decoder))
        case "terminal_detach": self = .`terminal_detach`(try EventTerminalDetach(from: decoder))
        case "terminal_kill": self = .`terminal_kill`(try EventTerminalKill(from: decoder))
        case "terminal_resize": self = .`terminal_resize`(try EventTerminalResize(from: decoder))
        case "push_register": self = .`push_register`(try EventPushRegister(from: decoder))
        case "thread_rename": self = .`thread_rename`(try EventThreadRename(from: decoder))
        case "thread_close": self = .`thread_close`(try EventThreadClose(from: decoder))
        case "thread_sync": self = .`thread_sync`(try EventThreadSync(from: decoder))
        case "thread_create": self = .`thread_create`(try EventThreadCreate(from: decoder))
        case "new_chat_select": self = .`new_chat_select`(try EventNewChatSelect(from: decoder))
        case "workspace_create": self = .`workspace_create`(try EventWorkspaceCreate(from: decoder))
        case "workspace_rename": self = .`workspace_rename`(try EventWorkspaceRename(from: decoder))
        case "workspace_archive": self = .`workspace_archive`(try EventWorkspaceArchive(from: decoder))
        case "workspace_close": self = .`workspace_close`(try EventWorkspaceClose(from: decoder))
        case "directory_list": self = .`directory_list`(try EventDirectoryList(from: decoder))
        case "file_open": self = .`file_open`(try EventFileOpen(from: decoder))
        case "push_received": self = .`push_received`(try EventPushReceived(from: decoder))
        case "terminal_input": self = .`terminal_input`(try EventTerminalInput(from: decoder))
        default: throw DecodingError.dataCorruptedError(forKey: .type, in: c, debugDescription: "Unknown model tag")
        }
    }
    func encode(to encoder: Encoder) throws {
        switch self {
        case .`sign_out`(let value): try value.encode(to: encoder)
        case .`forget_host`(let value): try value.encode(to: encoder)
        case .`start`(let value): try value.encode(to: encoder)
        case .`foreground`(let value): try value.encode(to: encoder)
        case .`background`(let value): try value.encode(to: encoder)
        case .`shutdown`(let value): try value.encode(to: encoder)
        case .`network_changed`(let value): try value.encode(to: encoder)
        case .`http_response`(let value): try value.encode(to: encoder)
        case .`ws_open`(let value): try value.encode(to: encoder)
        case .`ws_message`(let value): try value.encode(to: encoder)
        case .`ws_closed`(let value): try value.encode(to: encoder)
        case .`timer_fired`(let value): try value.encode(to: encoder)
        case .`secure_store_value`(let value): try value.encode(to: encoder)
        case .`secure_store_done`(let value): try value.encode(to: encoder)
        case .`tls_peer`(let value): try value.encode(to: encoder)
        case .`terminal_applied`(let value): try value.encode(to: encoder)
        case .`terminal_reply`(let value): try value.encode(to: encoder)
        case .`pair`(let value): try value.encode(to: encoder)
        case .`trust_decision`(let value): try value.encode(to: encoder)
        case .`retry_connection`(let value): try value.encode(to: encoder)
        case .`focus`(let value): try value.encode(to: encoder)
        case .`history_search`(let value): try value.encode(to: encoder)
        case .`history_load_more`(let value): try value.encode(to: encoder)
        case .`shell_confirm`(let value): try value.encode(to: encoder)
        case .`terminal_create`(let value): try value.encode(to: encoder)
        case .`thread_open`(let value): try value.encode(to: encoder)
        case .`thread_load_older`(let value): try value.encode(to: encoder)
        case .`draft_set`(let value): try value.encode(to: encoder)
        case .`composer_select`(let value): try value.encode(to: encoder)
        case .`send`(let value): try value.encode(to: encoder)
        case .`turn_cancel`(let value): try value.encode(to: encoder)
        case .`followup_submit`(let value): try value.encode(to: encoder)
        case .`followup_retry`(let value): try value.encode(to: encoder)
        case .`followup_pull_back`(let value): try value.encode(to: encoder)
        case .`followup_cancel`(let value): try value.encode(to: encoder)
        case .`approval_decide`(let value): try value.encode(to: encoder)
        case .`shell_prepare`(let value): try value.encode(to: encoder)
        case .`slash_search`(let value): try value.encode(to: encoder)
        case .`slash_run`(let value): try value.encode(to: encoder)
        case .`mention_search`(let value): try value.encode(to: encoder)
        case .`terminal_attach`(let value): try value.encode(to: encoder)
        case .`terminal_detach`(let value): try value.encode(to: encoder)
        case .`terminal_kill`(let value): try value.encode(to: encoder)
        case .`terminal_resize`(let value): try value.encode(to: encoder)
        case .`push_register`(let value): try value.encode(to: encoder)
        case .`thread_rename`(let value): try value.encode(to: encoder)
        case .`thread_close`(let value): try value.encode(to: encoder)
        case .`thread_sync`(let value): try value.encode(to: encoder)
        case .`thread_create`(let value): try value.encode(to: encoder)
        case .`new_chat_select`(let value): try value.encode(to: encoder)
        case .`workspace_create`(let value): try value.encode(to: encoder)
        case .`workspace_rename`(let value): try value.encode(to: encoder)
        case .`workspace_archive`(let value): try value.encode(to: encoder)
        case .`workspace_close`(let value): try value.encode(to: encoder)
        case .`directory_list`(let value): try value.encode(to: encoder)
        case .`file_open`(let value): try value.encode(to: encoder)
        case .`push_received`(let value): try value.encode(to: encoder)
        case .`terminal_input`(let value): try value.encode(to: encoder)
        }
    }
}

struct EventSignOut: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `host_id`: String
}

extension EventSignOut {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `host_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`host_id` = try c.decode(String.self, forKey: .`host_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`host_id`, forKey: .`host_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("sign_out", forKey: .type)
    }
}

struct EventForgetHost: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `host_id`: String
}

extension EventForgetHost {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `host_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`host_id` = try c.decode(String.self, forKey: .`host_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`host_id`, forKey: .`host_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("forget_host", forKey: .type)
    }
}

struct EventStart: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `foreground`: Bool
    var `network_available`: Bool
}

extension EventStart {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `foreground`
        case `network_available`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`foreground` = try c.decode(Bool.self, forKey: .`foreground`)
        self.`network_available` = try c.decode(Bool.self, forKey: .`network_available`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`foreground`, forKey: .`foreground`)
        try c.encode(self.`network_available`, forKey: .`network_available`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("start", forKey: .type)
    }
}

struct EventForeground: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
}

extension EventForeground {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("foreground", forKey: .type)
    }
}

struct EventBackground: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
}

extension EventBackground {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("background", forKey: .type)
    }
}

struct EventShutdown: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
}

extension EventShutdown {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("shutdown", forKey: .type)
    }
}

struct EventNetworkChanged: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `available`: Bool
    var `network_id`: String
}

extension EventNetworkChanged {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `available`
        case `network_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`available` = try c.decode(Bool.self, forKey: .`available`)
        self.`network_id` = try c.decode(String.self, forKey: .`network_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`available`, forKey: .`available`)
        try c.encode(self.`network_id`, forKey: .`network_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("network_changed", forKey: .type)
    }
}

struct EventHttpResponse: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `effect_id`: String
    var `generation`: String
    var `status`: UInt16?
    var `headers`: [Header]
    var `body_base64`: String?
    var `error`: TransportFailure?
}

extension EventHttpResponse {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `effect_id`
        case `generation`
        case `status`
        case `headers`
        case `body_base64`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`status` = try c.decodeIfPresent(UInt16.self, forKey: .`status`)
        self.`headers` = try c.decode([Header].self, forKey: .`headers`)
        self.`body_base64` = try c.decodeIfPresent(String.self, forKey: .`body_base64`)
        self.`error` = try c.decodeIfPresent(TransportFailure.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`status`, forKey: .`status`)
        try c.encode(self.`headers`, forKey: .`headers`)
        try c.encode(self.`body_base64`, forKey: .`body_base64`)
        try c.encode(self.`error`, forKey: .`error`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("http_response", forKey: .type)
    }
}

struct EventWsOpen: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `socket_id`: String
    var `generation`: String
    var `protocol`: String
}

extension EventWsOpen {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `socket_id`
        case `generation`
        case `protocol`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`socket_id` = try c.decode(String.self, forKey: .`socket_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`protocol` = try c.decode(String.self, forKey: .`protocol`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`socket_id`, forKey: .`socket_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`protocol`, forKey: .`protocol`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("ws_open", forKey: .type)
    }
}

struct EventWsMessage: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `socket_id`: String
    var `generation`: String
    var `text`: String
}

extension EventWsMessage {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `socket_id`
        case `generation`
        case `text`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`socket_id` = try c.decode(String.self, forKey: .`socket_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`text` = try c.decode(String.self, forKey: .`text`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`socket_id`, forKey: .`socket_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`text`, forKey: .`text`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("ws_message", forKey: .type)
    }
}

struct EventWsClosed: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `socket_id`: String
    var `generation`: String
    var `code`: UInt16?
    var `clean`: Bool
    var `error`: TransportFailure?
}

extension EventWsClosed {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `socket_id`
        case `generation`
        case `code`
        case `clean`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`socket_id` = try c.decode(String.self, forKey: .`socket_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`code` = try c.decodeIfPresent(UInt16.self, forKey: .`code`)
        self.`clean` = try c.decode(Bool.self, forKey: .`clean`)
        self.`error` = try c.decodeIfPresent(TransportFailure.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`socket_id`, forKey: .`socket_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`code`, forKey: .`code`)
        try c.encode(self.`clean`, forKey: .`clean`)
        try c.encode(self.`error`, forKey: .`error`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("ws_closed", forKey: .type)
    }
}

struct EventTimerFired: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `timer_id`: String
    var `generation`: String
}

extension EventTimerFired {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `timer_id`
        case `generation`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`timer_id` = try c.decode(String.self, forKey: .`timer_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`timer_id`, forKey: .`timer_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("timer_fired", forKey: .type)
    }
}

struct EventSecureStoreValue: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `effect_id`: String
    var `generation`: String
    var `key`: String
    var `value_base64`: String?
    var `error`: PlatformFailure?
}

extension EventSecureStoreValue {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `effect_id`
        case `generation`
        case `key`
        case `value_base64`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`key` = try c.decode(String.self, forKey: .`key`)
        self.`value_base64` = try c.decodeIfPresent(String.self, forKey: .`value_base64`)
        self.`error` = try c.decodeIfPresent(PlatformFailure.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`key`, forKey: .`key`)
        try c.encode(self.`value_base64`, forKey: .`value_base64`)
        try c.encode(self.`error`, forKey: .`error`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("secure_store_value", forKey: .type)
    }
}

struct EventSecureStoreDone: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `effect_id`: String
    var `generation`: String
    var `key`: String
    var `error`: PlatformFailure?
}

extension EventSecureStoreDone {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `effect_id`
        case `generation`
        case `key`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`key` = try c.decode(String.self, forKey: .`key`)
        self.`error` = try c.decodeIfPresent(PlatformFailure.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`key`, forKey: .`key`)
        try c.encode(self.`error`, forKey: .`error`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("secure_store_done", forKey: .type)
    }
}

struct EventTlsPeer: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `effect_id`: String
    var `generation`: String
    var `origin`: String
    var `spki_sha256`: String
    var `system_trusted`: Bool
}

extension EventTlsPeer {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `effect_id`
        case `generation`
        case `origin`
        case `spki_sha256`
        case `system_trusted`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`origin` = try c.decode(String.self, forKey: .`origin`)
        self.`spki_sha256` = try c.decode(String.self, forKey: .`spki_sha256`)
        self.`system_trusted` = try c.decode(Bool.self, forKey: .`system_trusted`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`origin`, forKey: .`origin`)
        try c.encode(self.`spki_sha256`, forKey: .`spki_sha256`)
        try c.encode(self.`system_trusted`, forKey: .`system_trusted`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("tls_peer", forKey: .type)
    }
}

struct EventTerminalApplied: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `effect_id`: String
    var `generation`: String
    var `terminal_id`: String
    var `grid_revision`: String
    var `error`: PlatformFailure?
}

extension EventTerminalApplied {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `effect_id`
        case `generation`
        case `terminal_id`
        case `grid_revision`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`terminal_id` = try c.decode(String.self, forKey: .`terminal_id`)
        self.`grid_revision` = try c.decode(String.self, forKey: .`grid_revision`)
        self.`error` = try c.decodeIfPresent(PlatformFailure.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        try c.encode(self.`grid_revision`, forKey: .`grid_revision`)
        try c.encode(self.`error`, forKey: .`error`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("terminal_applied", forKey: .type)
    }
}

struct EventTerminalReply: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `terminal_id`: String
    var `bytes_base64`: String
}

extension EventTerminalReply {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `terminal_id`
        case `bytes_base64`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`terminal_id` = try c.decode(String.self, forKey: .`terminal_id`)
        self.`bytes_base64` = try c.decode(String.self, forKey: .`bytes_base64`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        try c.encode(self.`bytes_base64`, forKey: .`bytes_base64`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("terminal_reply", forKey: .type)
    }
}

struct EventPair: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `link`: String
    var `device_label`: String
    var `client_nonce`: String
}

extension EventPair {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `link`
        case `device_label`
        case `client_nonce`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`link` = try c.decode(String.self, forKey: .`link`)
        self.`device_label` = try c.decode(String.self, forKey: .`device_label`)
        self.`client_nonce` = try c.decode(String.self, forKey: .`client_nonce`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`link`, forKey: .`link`)
        try c.encode(self.`device_label`, forKey: .`device_label`)
        try c.encode(self.`client_nonce`, forKey: .`client_nonce`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("pair", forKey: .type)
    }
}

struct EventTrustDecision: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `proposal_id`: String
    var `accept`: Bool
}

extension EventTrustDecision {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `proposal_id`
        case `accept`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`proposal_id` = try c.decode(String.self, forKey: .`proposal_id`)
        self.`accept` = try c.decode(Bool.self, forKey: .`accept`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`proposal_id`, forKey: .`proposal_id`)
        try c.encode(self.`accept`, forKey: .`accept`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("trust_decision", forKey: .type)
    }
}

struct EventRetryConnection: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
}

extension EventRetryConnection {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("retry_connection", forKey: .type)
    }
}

struct EventFocus: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String?
    var `thread_id`: String?
    var `terminal_id`: String?
}

extension EventFocus {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `terminal_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decodeIfPresent(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decodeIfPresent(String.self, forKey: .`thread_id`)
        self.`terminal_id` = try c.decodeIfPresent(String.self, forKey: .`terminal_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("focus", forKey: .type)
    }
}

struct EventHistorySearch: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `query`: String
    var `workspace_id`: String?
}

extension EventHistorySearch {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `query`
        case `workspace_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`query` = try c.decode(String.self, forKey: .`query`)
        self.`workspace_id` = try c.decodeIfPresent(String.self, forKey: .`workspace_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`query`, forKey: .`query`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("history_search", forKey: .type)
    }
}

struct EventHistoryLoadMore: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
}

extension EventHistoryLoadMore {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("history_load_more", forKey: .type)
    }
}

struct EventShellConfirm: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `confirmation_id`: String
    var `accept`: Bool
}

extension EventShellConfirm {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `confirmation_id`
        case `accept`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`confirmation_id` = try c.decode(String.self, forKey: .`confirmation_id`)
        self.`accept` = try c.decode(Bool.self, forKey: .`accept`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`confirmation_id`, forKey: .`confirmation_id`)
        try c.encode(self.`accept`, forKey: .`accept`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("shell_confirm", forKey: .type)
    }
}

struct EventTerminalCreate: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `cwd`: String?
    var `cols`: UInt16
    var `rows`: UInt16
}

extension EventTerminalCreate {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `cwd`
        case `cols`
        case `rows`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`cwd` = try c.decodeIfPresent(String.self, forKey: .`cwd`)
        self.`cols` = try c.decode(UInt16.self, forKey: .`cols`)
        self.`rows` = try c.decode(UInt16.self, forKey: .`rows`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`cwd`, forKey: .`cwd`)
        try c.encode(self.`cols`, forKey: .`cols`)
        try c.encode(self.`rows`, forKey: .`rows`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("terminal_create", forKey: .type)
    }
}

struct EventThreadOpen: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
}

extension EventThreadOpen {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("thread_open", forKey: .type)
    }
}

struct EventThreadLoadOlder: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
}

extension EventThreadLoadOlder {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("thread_load_older", forKey: .type)
    }
}

struct EventDraftSet: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `text`: String
    var `attachments`: [AttachmentInput]
}

extension EventDraftSet {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `text`
        case `attachments`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`text` = try c.decode(String.self, forKey: .`text`)
        self.`attachments` = try c.decode([AttachmentInput].self, forKey: .`attachments`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`text`, forKey: .`text`)
        try c.encode(self.`attachments`, forKey: .`attachments`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("draft_set", forKey: .type)
    }
}

struct EventComposerSelect: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `provider`: String?
    var `model`: String?
    var `effort`: String?
    var `access`: String?
    var `speed`: String?
}

extension EventComposerSelect {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `provider`
        case `model`
        case `effort`
        case `access`
        case `speed`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`provider` = try c.decodeIfPresent(String.self, forKey: .`provider`)
        self.`model` = try c.decodeIfPresent(String.self, forKey: .`model`)
        self.`effort` = try c.decodeIfPresent(String.self, forKey: .`effort`)
        self.`access` = try c.decodeIfPresent(String.self, forKey: .`access`)
        self.`speed` = try c.decodeIfPresent(String.self, forKey: .`speed`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`provider`, forKey: .`provider`)
        try c.encode(self.`model`, forKey: .`model`)
        try c.encode(self.`effort`, forKey: .`effort`)
        try c.encode(self.`access`, forKey: .`access`)
        try c.encode(self.`speed`, forKey: .`speed`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("composer_select", forKey: .type)
    }
}

struct EventSend: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `draft_revision`: String
}

extension EventSend {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `draft_revision`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`draft_revision` = try c.decode(String.self, forKey: .`draft_revision`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`draft_revision`, forKey: .`draft_revision`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("send", forKey: .type)
    }
}

struct EventTurnCancel: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `turn_id`: String
}

extension EventTurnCancel {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `turn_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`turn_id` = try c.decode(String.self, forKey: .`turn_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`turn_id`, forKey: .`turn_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("turn_cancel", forKey: .type)
    }
}

enum EventFollowupSubmitKind: String, Codable {
    case `queue`
    case `steer`
}

struct EventFollowupSubmit: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `draft_revision`: String
    var `kind`: EventFollowupSubmitKind
}

extension EventFollowupSubmit {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `draft_revision`
        case `kind`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`draft_revision` = try c.decode(String.self, forKey: .`draft_revision`)
        self.`kind` = try c.decode(EventFollowupSubmitKind.self, forKey: .`kind`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`draft_revision`, forKey: .`draft_revision`)
        try c.encode(self.`kind`, forKey: .`kind`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("followup_submit", forKey: .type)
    }
}

struct EventFollowupRetry: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `followup_id`: String
}

extension EventFollowupRetry {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `followup_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`followup_id` = try c.decode(String.self, forKey: .`followup_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`followup_id`, forKey: .`followup_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("followup_retry", forKey: .type)
    }
}

struct EventFollowupPullBack: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `followup_id`: String
}

extension EventFollowupPullBack {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `followup_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`followup_id` = try c.decode(String.self, forKey: .`followup_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`followup_id`, forKey: .`followup_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("followup_pull_back", forKey: .type)
    }
}

struct EventFollowupCancel: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `followup_id`: String
}

extension EventFollowupCancel {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `followup_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`followup_id` = try c.decode(String.self, forKey: .`followup_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`followup_id`, forKey: .`followup_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("followup_cancel", forKey: .type)
    }
}

enum EventApprovalDecideDecision: String, Codable {
    case `approve`
    case `deny`
}

struct EventApprovalDecide: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `turn_id`: String
    var `call_id`: String
    var `decision`: EventApprovalDecideDecision
}

extension EventApprovalDecide {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `turn_id`
        case `call_id`
        case `decision`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`turn_id` = try c.decode(String.self, forKey: .`turn_id`)
        self.`call_id` = try c.decode(String.self, forKey: .`call_id`)
        self.`decision` = try c.decode(EventApprovalDecideDecision.self, forKey: .`decision`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`turn_id`, forKey: .`turn_id`)
        try c.encode(self.`call_id`, forKey: .`call_id`)
        try c.encode(self.`decision`, forKey: .`decision`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("approval_decide", forKey: .type)
    }
}

struct EventShellPrepare: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `command`: String
}

extension EventShellPrepare {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `command`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`command` = try c.decode(String.self, forKey: .`command`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`command`, forKey: .`command`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("shell_prepare", forKey: .type)
    }
}

struct EventSlashSearch: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `query`: String
}

extension EventSlashSearch {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `query`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`query` = try c.decode(String.self, forKey: .`query`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`query`, forKey: .`query`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("slash_search", forKey: .type)
    }
}

struct EventSlashRun: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `command`: String
    var `args`: String
}

extension EventSlashRun {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `command`
        case `args`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`command` = try c.decode(String.self, forKey: .`command`)
        self.`args` = try c.decode(String.self, forKey: .`args`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`command`, forKey: .`command`)
        try c.encode(self.`args`, forKey: .`args`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("slash_run", forKey: .type)
    }
}

struct EventMentionSearch: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `query`: String
}

extension EventMentionSearch {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `query`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`query` = try c.decode(String.self, forKey: .`query`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`query`, forKey: .`query`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("mention_search", forKey: .type)
    }
}

struct EventTerminalAttach: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `terminal_id`: String
}

extension EventTerminalAttach {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `terminal_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`terminal_id` = try c.decode(String.self, forKey: .`terminal_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("terminal_attach", forKey: .type)
    }
}

struct EventTerminalDetach: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `terminal_id`: String
}

extension EventTerminalDetach {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `terminal_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`terminal_id` = try c.decode(String.self, forKey: .`terminal_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("terminal_detach", forKey: .type)
    }
}

struct EventTerminalKill: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `terminal_id`: String
}

extension EventTerminalKill {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `terminal_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`terminal_id` = try c.decode(String.self, forKey: .`terminal_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("terminal_kill", forKey: .type)
    }
}

struct EventTerminalResize: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `terminal_id`: String
    var `cols`: UInt16
    var `rows`: UInt16
}

extension EventTerminalResize {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `terminal_id`
        case `cols`
        case `rows`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`terminal_id` = try c.decode(String.self, forKey: .`terminal_id`)
        self.`cols` = try c.decode(UInt16.self, forKey: .`cols`)
        self.`rows` = try c.decode(UInt16.self, forKey: .`rows`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        try c.encode(self.`cols`, forKey: .`cols`)
        try c.encode(self.`rows`, forKey: .`rows`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("terminal_resize", forKey: .type)
    }
}

struct EventPushRegister: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `platform`: String
    var `send_token`: String
    var `key_seed_base64`: String
}

extension EventPushRegister {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `platform`
        case `send_token`
        case `key_seed_base64`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`platform` = try c.decode(String.self, forKey: .`platform`)
        self.`send_token` = try c.decode(String.self, forKey: .`send_token`)
        self.`key_seed_base64` = try c.decode(String.self, forKey: .`key_seed_base64`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`platform`, forKey: .`platform`)
        try c.encode(self.`send_token`, forKey: .`send_token`)
        try c.encode(self.`key_seed_base64`, forKey: .`key_seed_base64`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("push_register", forKey: .type)
    }
}

struct EventThreadRename: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
    var `title`: String
}

extension EventThreadRename {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
        case `title`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`title` = try c.decode(String.self, forKey: .`title`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`title`, forKey: .`title`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("thread_rename", forKey: .type)
    }
}

struct EventThreadClose: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
}

extension EventThreadClose {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("thread_close", forKey: .type)
    }
}

struct EventThreadSync: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `thread_id`: String
}

extension EventThreadSync {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `thread_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("thread_sync", forKey: .type)
    }
}

struct EventThreadCreate: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `provider`: String
    var `model`: String? = nil
    var `effort`: String? = nil
    var `access`: String? = nil
    var `speed`: String? = nil
}

extension EventThreadCreate {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `provider`
        case `model`
        case `effort`
        case `access`
        case `speed`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`provider` = try c.decode(String.self, forKey: .`provider`)
        if !c.contains(.`model`) { self.`model` = nil } else {
        self.`model` = try c.decodeIfPresent(String.self, forKey: .`model`)
        }
        if !c.contains(.`effort`) { self.`effort` = nil } else {
        self.`effort` = try c.decodeIfPresent(String.self, forKey: .`effort`)
        }
        if !c.contains(.`access`) { self.`access` = nil } else {
        self.`access` = try c.decodeIfPresent(String.self, forKey: .`access`)
        }
        if !c.contains(.`speed`) { self.`speed` = nil } else {
        self.`speed` = try c.decodeIfPresent(String.self, forKey: .`speed`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`provider`, forKey: .`provider`)
        try c.encode(self.`model`, forKey: .`model`)
        try c.encode(self.`effort`, forKey: .`effort`)
        try c.encode(self.`access`, forKey: .`access`)
        try c.encode(self.`speed`, forKey: .`speed`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("thread_create", forKey: .type)
    }
}

struct EventNewChatSelect: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `provider`: String? = nil
    var `model`: String? = nil
    var `effort`: String? = nil
    var `access`: String? = nil
    var `speed`: String? = nil
}

extension EventNewChatSelect {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `provider`
        case `model`
        case `effort`
        case `access`
        case `speed`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        if !c.contains(.`provider`) { self.`provider` = nil } else {
        self.`provider` = try c.decodeIfPresent(String.self, forKey: .`provider`)
        }
        if !c.contains(.`model`) { self.`model` = nil } else {
        self.`model` = try c.decodeIfPresent(String.self, forKey: .`model`)
        }
        if !c.contains(.`effort`) { self.`effort` = nil } else {
        self.`effort` = try c.decodeIfPresent(String.self, forKey: .`effort`)
        }
        if !c.contains(.`access`) { self.`access` = nil } else {
        self.`access` = try c.decodeIfPresent(String.self, forKey: .`access`)
        }
        if !c.contains(.`speed`) { self.`speed` = nil } else {
        self.`speed` = try c.decodeIfPresent(String.self, forKey: .`speed`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`provider`, forKey: .`provider`)
        try c.encode(self.`model`, forKey: .`model`)
        try c.encode(self.`effort`, forKey: .`effort`)
        try c.encode(self.`access`, forKey: .`access`)
        try c.encode(self.`speed`, forKey: .`speed`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("new_chat_select", forKey: .type)
    }
}

struct EventWorkspaceCreate: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `path`: String
    var `label`: String? = nil
}

extension EventWorkspaceCreate {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `path`
        case `label`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`path` = try c.decode(String.self, forKey: .`path`)
        if !c.contains(.`label`) { self.`label` = nil } else {
        self.`label` = try c.decodeIfPresent(String.self, forKey: .`label`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`path`, forKey: .`path`)
        try c.encode(self.`label`, forKey: .`label`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("workspace_create", forKey: .type)
    }
}

struct EventWorkspaceRename: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `label`: String
}

extension EventWorkspaceRename {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `label`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`label` = try c.decode(String.self, forKey: .`label`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`label`, forKey: .`label`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("workspace_rename", forKey: .type)
    }
}

struct EventWorkspaceArchive: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
    var `archived`: Bool
}

extension EventWorkspaceArchive {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
        case `archived`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`archived` = try c.decode(Bool.self, forKey: .`archived`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`archived`, forKey: .`archived`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("workspace_archive", forKey: .type)
    }
}

struct EventWorkspaceClose: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `workspace_id`: String
}

extension EventWorkspaceClose {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `workspace_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("workspace_close", forKey: .type)
    }
}

struct EventDirectoryList: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `path`: String? = nil
}

extension EventDirectoryList {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `path`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        if !c.contains(.`path`) { self.`path` = nil } else {
        self.`path` = try c.decodeIfPresent(String.self, forKey: .`path`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`path`, forKey: .`path`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("directory_list", forKey: .type)
    }
}

struct EventFileOpen: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `path`: String
    var `kind`: FileKind
    var `max_bytes`: UInt32
}

extension EventFileOpen {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `path`
        case `kind`
        case `max_bytes`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`path` = try c.decode(String.self, forKey: .`path`)
        self.`kind` = try c.decode(FileKind.self, forKey: .`kind`)
        self.`max_bytes` = try c.decode(UInt32.self, forKey: .`max_bytes`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`path`, forKey: .`path`)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`max_bytes`, forKey: .`max_bytes`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("file_open", forKey: .type)
    }
}

struct EventPushReceived: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `workspace_id`: String
    var `thread_id`: String
    var `turn_id`: String
    var `kind`: String
}

extension EventPushReceived {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `workspace_id`
        case `thread_id`
        case `turn_id`
        case `kind`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`turn_id` = try c.decode(String.self, forKey: .`turn_id`)
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`turn_id`, forKey: .`turn_id`)
        try c.encode(self.`kind`, forKey: .`kind`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("push_received", forKey: .type)
    }
}

enum EventTerminalInputInputKind: String, Codable {
    case `text`
    case `key`
    case `paste`
}

struct EventTerminalInputInput: Codable {
    var `kind`: EventTerminalInputInputKind
    var `text`: String? = nil
    var `key`: String? = nil
    var `ctrl`: Bool
    var `alt`: Bool
    var `shift`: Bool
}

extension EventTerminalInputInput {
    private enum CodingKeys: String, CodingKey {
        case `kind`
        case `text`
        case `key`
        case `ctrl`
        case `alt`
        case `shift`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`kind` = try c.decode(EventTerminalInputInputKind.self, forKey: .`kind`)
        if !c.contains(.`text`) { self.`text` = nil } else {
        self.`text` = try c.decodeIfPresent(String.self, forKey: .`text`)
        }
        if !c.contains(.`key`) { self.`key` = nil } else {
        self.`key` = try c.decodeIfPresent(String.self, forKey: .`key`)
        }
        self.`ctrl` = try c.decode(Bool.self, forKey: .`ctrl`)
        self.`alt` = try c.decode(Bool.self, forKey: .`alt`)
        self.`shift` = try c.decode(Bool.self, forKey: .`shift`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`text`, forKey: .`text`)
        try c.encode(self.`key`, forKey: .`key`)
        try c.encode(self.`ctrl`, forKey: .`ctrl`)
        try c.encode(self.`alt`, forKey: .`alt`)
        try c.encode(self.`shift`, forKey: .`shift`)
    }
}

struct EventTerminalInput: Codable {
    var `api_version`: UInt32 = 1
    var `now_ms`: Int64
    var `wall_time_ms`: Int64
    var `intent_id`: String
    var `terminal_id`: String
    var `vt_modes`: VtModes
    var `input`: EventTerminalInputInput
}

extension EventTerminalInput {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `now_ms`
        case `wall_time_ms`
        case `intent_id`
        case `terminal_id`
        case `vt_modes`
        case `input`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`api_version`) { self.`api_version` = 1 } else {
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        }
        self.`now_ms` = try c.decode(Int64.self, forKey: .`now_ms`)
        self.`wall_time_ms` = try c.decode(Int64.self, forKey: .`wall_time_ms`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`terminal_id` = try c.decode(String.self, forKey: .`terminal_id`)
        self.`vt_modes` = try c.decode(VtModes.self, forKey: .`vt_modes`)
        self.`input` = try c.decode(EventTerminalInputInput.self, forKey: .`input`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`now_ms`, forKey: .`now_ms`)
        try c.encode(self.`wall_time_ms`, forKey: .`wall_time_ms`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        try c.encode(self.`vt_modes`, forKey: .`vt_modes`)
        try c.encode(self.`input`, forKey: .`input`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("terminal_input", forKey: .type)
    }
}

enum Effect: Codable {
    case `http_request`(EffectHttpRequest)
    case `http_cancel`(EffectHttpCancel)
    case `ws_open`(EffectWsOpen)
    case `ws_send`(EffectWsSend)
    case `ws_close`(EffectWsClose)
    case `set_timer`(EffectSetTimer)
    case `cancel_timer`(EffectCancelTimer)
    case `secure_store_get`(EffectSecureStoreGet)
    case `secure_store_put`(EffectSecureStorePut)
    case `secure_store_delete`(EffectSecureStoreDelete)
    case `state_changed`(EffectStateChanged)
    case `notify`(EffectNotify)
    case `log`(EffectLog)
    case `tls_probe`(EffectTlsProbe)
    case `terminal_output`(EffectTerminalOutput)
    case `file_fetch`(EffectFileFetch)
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: ModelDiscriminator.self)
        let tag = try c.decode(String.self, forKey: .type)
        switch tag {
        case "http_request": self = .`http_request`(try EffectHttpRequest(from: decoder))
        case "http_cancel": self = .`http_cancel`(try EffectHttpCancel(from: decoder))
        case "ws_open": self = .`ws_open`(try EffectWsOpen(from: decoder))
        case "ws_send": self = .`ws_send`(try EffectWsSend(from: decoder))
        case "ws_close": self = .`ws_close`(try EffectWsClose(from: decoder))
        case "set_timer": self = .`set_timer`(try EffectSetTimer(from: decoder))
        case "cancel_timer": self = .`cancel_timer`(try EffectCancelTimer(from: decoder))
        case "secure_store_get": self = .`secure_store_get`(try EffectSecureStoreGet(from: decoder))
        case "secure_store_put": self = .`secure_store_put`(try EffectSecureStorePut(from: decoder))
        case "secure_store_delete": self = .`secure_store_delete`(try EffectSecureStoreDelete(from: decoder))
        case "state_changed": self = .`state_changed`(try EffectStateChanged(from: decoder))
        case "notify": self = .`notify`(try EffectNotify(from: decoder))
        case "log": self = .`log`(try EffectLog(from: decoder))
        case "tls_probe": self = .`tls_probe`(try EffectTlsProbe(from: decoder))
        case "terminal_output": self = .`terminal_output`(try EffectTerminalOutput(from: decoder))
        case "file_fetch": self = .`file_fetch`(try EffectFileFetch(from: decoder))
        default: throw DecodingError.dataCorruptedError(forKey: .type, in: c, debugDescription: "Unknown model tag")
        }
    }
    func encode(to encoder: Encoder) throws {
        switch self {
        case .`http_request`(let value): try value.encode(to: encoder)
        case .`http_cancel`(let value): try value.encode(to: encoder)
        case .`ws_open`(let value): try value.encode(to: encoder)
        case .`ws_send`(let value): try value.encode(to: encoder)
        case .`ws_close`(let value): try value.encode(to: encoder)
        case .`set_timer`(let value): try value.encode(to: encoder)
        case .`cancel_timer`(let value): try value.encode(to: encoder)
        case .`secure_store_get`(let value): try value.encode(to: encoder)
        case .`secure_store_put`(let value): try value.encode(to: encoder)
        case .`secure_store_delete`(let value): try value.encode(to: encoder)
        case .`state_changed`(let value): try value.encode(to: encoder)
        case .`notify`(let value): try value.encode(to: encoder)
        case .`log`(let value): try value.encode(to: encoder)
        case .`tls_probe`(let value): try value.encode(to: encoder)
        case .`terminal_output`(let value): try value.encode(to: encoder)
        case .`file_fetch`(let value): try value.encode(to: encoder)
        }
    }
}

struct EffectHttpRequest: Codable {
    var `effect_id`: String
    var `generation`: String
    var `method`: String
    var `url`: String
    var `headers`: [Header]
    var `body_base64`: String?
    var `timeout_ms`: UInt32
    var `max_response_bytes`: UInt32
    var `tls`: Tls
}

extension EffectHttpRequest {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `method`
        case `url`
        case `headers`
        case `body_base64`
        case `timeout_ms`
        case `max_response_bytes`
        case `tls`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`method` = try c.decode(String.self, forKey: .`method`)
        self.`url` = try c.decode(String.self, forKey: .`url`)
        self.`headers` = try c.decode([Header].self, forKey: .`headers`)
        self.`body_base64` = try c.decodeIfPresent(String.self, forKey: .`body_base64`)
        self.`timeout_ms` = try c.decode(UInt32.self, forKey: .`timeout_ms`)
        self.`max_response_bytes` = try c.decode(UInt32.self, forKey: .`max_response_bytes`)
        self.`tls` = try c.decode(Tls.self, forKey: .`tls`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`method`, forKey: .`method`)
        try c.encode(self.`url`, forKey: .`url`)
        try c.encode(self.`headers`, forKey: .`headers`)
        try c.encode(self.`body_base64`, forKey: .`body_base64`)
        try c.encode(self.`timeout_ms`, forKey: .`timeout_ms`)
        try c.encode(self.`max_response_bytes`, forKey: .`max_response_bytes`)
        try c.encode(self.`tls`, forKey: .`tls`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("http_request", forKey: .type)
    }
}

struct EffectHttpCancel: Codable {
    var `effect_id`: String
    var `generation`: String
    var `request_id`: String
}

extension EffectHttpCancel {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `request_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`request_id` = try c.decode(String.self, forKey: .`request_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`request_id`, forKey: .`request_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("http_cancel", forKey: .type)
    }
}

struct EffectWsOpen: Codable {
    var `effect_id`: String
    var `generation`: String
    var `url`: String
    var `protocols`: [String]
    var `tls`: Tls
    var `max_message_bytes`: UInt32
}

extension EffectWsOpen {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `url`
        case `protocols`
        case `tls`
        case `max_message_bytes`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`url` = try c.decode(String.self, forKey: .`url`)
        self.`protocols` = try c.decode([String].self, forKey: .`protocols`)
        self.`tls` = try c.decode(Tls.self, forKey: .`tls`)
        self.`max_message_bytes` = try c.decode(UInt32.self, forKey: .`max_message_bytes`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`url`, forKey: .`url`)
        try c.encode(self.`protocols`, forKey: .`protocols`)
        try c.encode(self.`tls`, forKey: .`tls`)
        try c.encode(self.`max_message_bytes`, forKey: .`max_message_bytes`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("ws_open", forKey: .type)
    }
}

struct EffectWsSend: Codable {
    var `effect_id`: String
    var `generation`: String
    var `socket_id`: String
    var `text`: String
}

extension EffectWsSend {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `socket_id`
        case `text`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`socket_id` = try c.decode(String.self, forKey: .`socket_id`)
        self.`text` = try c.decode(String.self, forKey: .`text`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`socket_id`, forKey: .`socket_id`)
        try c.encode(self.`text`, forKey: .`text`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("ws_send", forKey: .type)
    }
}

struct EffectWsClose: Codable {
    var `effect_id`: String
    var `generation`: String
    var `socket_id`: String
    var `code`: UInt16
}

extension EffectWsClose {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `socket_id`
        case `code`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`socket_id` = try c.decode(String.self, forKey: .`socket_id`)
        self.`code` = try c.decode(UInt16.self, forKey: .`code`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`socket_id`, forKey: .`socket_id`)
        try c.encode(self.`code`, forKey: .`code`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("ws_close", forKey: .type)
    }
}

struct EffectSetTimer: Codable {
    var `effect_id`: String
    var `generation`: String
    var `timer_id`: String
    var `delay_ms`: UInt32
    var `purpose`: String
}

extension EffectSetTimer {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `timer_id`
        case `delay_ms`
        case `purpose`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`timer_id` = try c.decode(String.self, forKey: .`timer_id`)
        self.`delay_ms` = try c.decode(UInt32.self, forKey: .`delay_ms`)
        self.`purpose` = try c.decode(String.self, forKey: .`purpose`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`timer_id`, forKey: .`timer_id`)
        try c.encode(self.`delay_ms`, forKey: .`delay_ms`)
        try c.encode(self.`purpose`, forKey: .`purpose`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("set_timer", forKey: .type)
    }
}

struct EffectCancelTimer: Codable {
    var `effect_id`: String
    var `generation`: String
    var `timer_id`: String
}

extension EffectCancelTimer {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `timer_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`timer_id` = try c.decode(String.self, forKey: .`timer_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`timer_id`, forKey: .`timer_id`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("cancel_timer", forKey: .type)
    }
}

struct EffectSecureStoreGet: Codable {
    var `effect_id`: String
    var `generation`: String
    var `key`: String
}

extension EffectSecureStoreGet {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `key`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`key` = try c.decode(String.self, forKey: .`key`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`key`, forKey: .`key`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("secure_store_get", forKey: .type)
    }
}

struct EffectSecureStorePut: Codable {
    var `effect_id`: String
    var `generation`: String
    var `key`: String
    var `value_base64`: String
}

extension EffectSecureStorePut {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `key`
        case `value_base64`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`key` = try c.decode(String.self, forKey: .`key`)
        self.`value_base64` = try c.decode(String.self, forKey: .`value_base64`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`key`, forKey: .`key`)
        try c.encode(self.`value_base64`, forKey: .`value_base64`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("secure_store_put", forKey: .type)
    }
}

struct EffectSecureStoreDelete: Codable {
    var `effect_id`: String
    var `generation`: String
    var `key`: String
}

extension EffectSecureStoreDelete {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `key`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`key` = try c.decode(String.self, forKey: .`key`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`key`, forKey: .`key`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("secure_store_delete", forKey: .type)
    }
}

struct EffectStateChanged: Codable {
    var `effect_id`: String
    var `generation`: String
    var `revision`: String
    var `scopes`: [String]
}

extension EffectStateChanged {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `revision`
        case `scopes`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`scopes` = try c.decode([String].self, forKey: .`scopes`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`scopes`, forKey: .`scopes`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("state_changed", forKey: .type)
    }
}

struct EffectNotifyTarget: Codable {
    var `host_id`: String
    var `workspace_id`: String?
    var `thread_id`: String?
}

extension EffectNotifyTarget {
    private enum CodingKeys: String, CodingKey {
        case `host_id`
        case `workspace_id`
        case `thread_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`host_id` = try c.decode(String.self, forKey: .`host_id`)
        self.`workspace_id` = try c.decodeIfPresent(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decodeIfPresent(String.self, forKey: .`thread_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`host_id`, forKey: .`host_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
    }
}

struct EffectNotify: Codable {
    var `effect_id`: String
    var `generation`: String
    var `notification_id`: String
    var `kind`: String
    var `title`: String
    var `body`: String
    var `target`: EffectNotifyTarget
    var `actions`: [String]
}

extension EffectNotify {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `notification_id`
        case `kind`
        case `title`
        case `body`
        case `target`
        case `actions`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`notification_id` = try c.decode(String.self, forKey: .`notification_id`)
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
        self.`title` = try c.decode(String.self, forKey: .`title`)
        self.`body` = try c.decode(String.self, forKey: .`body`)
        self.`target` = try c.decode(EffectNotifyTarget.self, forKey: .`target`)
        self.`actions` = try c.decode([String].self, forKey: .`actions`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`notification_id`, forKey: .`notification_id`)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`title`, forKey: .`title`)
        try c.encode(self.`body`, forKey: .`body`)
        try c.encode(self.`target`, forKey: .`target`)
        try c.encode(self.`actions`, forKey: .`actions`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("notify", forKey: .type)
    }
}

struct EffectLog: Codable {
    var `effect_id`: String
    var `generation`: String
    var `level`: String
    var `code`: String
    var `fields`: JSONValue
}

extension EffectLog {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `level`
        case `code`
        case `fields`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`level` = try c.decode(String.self, forKey: .`level`)
        self.`code` = try c.decode(String.self, forKey: .`code`)
        self.`fields` = try c.decode(JSONValue.self, forKey: .`fields`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`level`, forKey: .`level`)
        try c.encode(self.`code`, forKey: .`code`)
        try c.encode(self.`fields`, forKey: .`fields`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("log", forKey: .type)
    }
}

struct EffectTlsProbe: Codable {
    var `effect_id`: String
    var `generation`: String
    var `origin`: String
}

extension EffectTlsProbe {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `origin`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`origin` = try c.decode(String.self, forKey: .`origin`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`origin`, forKey: .`origin`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("tls_probe", forKey: .type)
    }
}

struct EffectTerminalOutput: Codable {
    var `effect_id`: String
    var `generation`: String
    var `terminal_id`: String
    var `reset`: Bool
    var `bytes_base64`: String
    var `next_offset`: String
}

extension EffectTerminalOutput {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `terminal_id`
        case `reset`
        case `bytes_base64`
        case `next_offset`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`terminal_id` = try c.decode(String.self, forKey: .`terminal_id`)
        self.`reset` = try c.decode(Bool.self, forKey: .`reset`)
        self.`bytes_base64` = try c.decode(String.self, forKey: .`bytes_base64`)
        self.`next_offset` = try c.decode(String.self, forKey: .`next_offset`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        try c.encode(self.`reset`, forKey: .`reset`)
        try c.encode(self.`bytes_base64`, forKey: .`bytes_base64`)
        try c.encode(self.`next_offset`, forKey: .`next_offset`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("terminal_output", forKey: .type)
    }
}

struct EffectFileFetch: Codable {
    var `effect_id`: String
    var `generation`: String
    var `intent_id`: String
    var `url`: String
    var `headers`: [Header]
    var `timeout_ms`: UInt32
    var `max_response_bytes`: UInt32
    var `tls`: Tls
}

extension EffectFileFetch {
    private enum CodingKeys: String, CodingKey {
        case `effect_id`
        case `generation`
        case `intent_id`
        case `url`
        case `headers`
        case `timeout_ms`
        case `max_response_bytes`
        case `tls`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`effect_id` = try c.decode(String.self, forKey: .`effect_id`)
        self.`generation` = try c.decode(String.self, forKey: .`generation`)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`url` = try c.decode(String.self, forKey: .`url`)
        self.`headers` = try c.decode([Header].self, forKey: .`headers`)
        self.`timeout_ms` = try c.decode(UInt32.self, forKey: .`timeout_ms`)
        self.`max_response_bytes` = try c.decode(UInt32.self, forKey: .`max_response_bytes`)
        self.`tls` = try c.decode(Tls.self, forKey: .`tls`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`effect_id`, forKey: .`effect_id`)
        try c.encode(self.`generation`, forKey: .`generation`)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`url`, forKey: .`url`)
        try c.encode(self.`headers`, forKey: .`headers`)
        try c.encode(self.`timeout_ms`, forKey: .`timeout_ms`)
        try c.encode(self.`max_response_bytes`, forKey: .`max_response_bytes`)
        try c.encode(self.`tls`, forKey: .`tls`)
        var tag = encoder.container(keyedBy: ModelDiscriminator.self)
        try tag.encode("file_fetch", forKey: .type)
    }
}

struct EffectBatch: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `effects`: [Effect]
}

extension EffectBatch {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `effects`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`effects` = try c.decode([Effect].self, forKey: .`effects`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`effects`, forKey: .`effects`)
    }
}

struct TrustProposal: Codable {
    var `id`: String
    var `origin`: String
    var `spki_sha256`: String
    var `runtime_id`: String?
}

extension TrustProposal {
    private enum CodingKeys: String, CodingKey {
        case `id`
        case `origin`
        case `spki_sha256`
        case `runtime_id`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`id` = try c.decode(String.self, forKey: .`id`)
        self.`origin` = try c.decode(String.self, forKey: .`origin`)
        self.`spki_sha256` = try c.decode(String.self, forKey: .`spki_sha256`)
        self.`runtime_id` = try c.decodeIfPresent(String.self, forKey: .`runtime_id`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`id`, forKey: .`id`)
        try c.encode(self.`origin`, forKey: .`origin`)
        try c.encode(self.`spki_sha256`, forKey: .`spki_sha256`)
        try c.encode(self.`runtime_id`, forKey: .`runtime_id`)
    }
}

struct HostView: Codable {
    var `host_id`: String
    var `label`: String
    var `https_url`: String?
    var `runtime_id`: String?
    var `instance_id`: String?
    var `phase`: String
    var `lifecycle`: Lifecycle
    var `auth_state`: String
    var `sync_state`: String
    var `capabilities`: [String]
    var `scopes`: [String]
    var `retry_at_ms`: Int64?
    var `trust_proposal`: TrustProposal?
    var `update_required`: Bool
    var `error`: LocalError?
}

extension HostView {
    private enum CodingKeys: String, CodingKey {
        case `host_id`
        case `label`
        case `https_url`
        case `runtime_id`
        case `instance_id`
        case `phase`
        case `lifecycle`
        case `auth_state`
        case `sync_state`
        case `capabilities`
        case `scopes`
        case `retry_at_ms`
        case `trust_proposal`
        case `update_required`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`host_id` = try c.decode(String.self, forKey: .`host_id`)
        self.`label` = try c.decode(String.self, forKey: .`label`)
        self.`https_url` = try c.decodeIfPresent(String.self, forKey: .`https_url`)
        self.`runtime_id` = try c.decodeIfPresent(String.self, forKey: .`runtime_id`)
        self.`instance_id` = try c.decodeIfPresent(String.self, forKey: .`instance_id`)
        self.`phase` = try c.decode(String.self, forKey: .`phase`)
        self.`lifecycle` = try c.decode(Lifecycle.self, forKey: .`lifecycle`)
        self.`auth_state` = try c.decode(String.self, forKey: .`auth_state`)
        self.`sync_state` = try c.decode(String.self, forKey: .`sync_state`)
        self.`capabilities` = try c.decode([String].self, forKey: .`capabilities`)
        self.`scopes` = try c.decode([String].self, forKey: .`scopes`)
        self.`retry_at_ms` = try c.decodeIfPresent(Int64.self, forKey: .`retry_at_ms`)
        self.`trust_proposal` = try c.decodeIfPresent(TrustProposal.self, forKey: .`trust_proposal`)
        self.`update_required` = try c.decode(Bool.self, forKey: .`update_required`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`host_id`, forKey: .`host_id`)
        try c.encode(self.`label`, forKey: .`label`)
        try c.encode(self.`https_url`, forKey: .`https_url`)
        try c.encode(self.`runtime_id`, forKey: .`runtime_id`)
        try c.encode(self.`instance_id`, forKey: .`instance_id`)
        try c.encode(self.`phase`, forKey: .`phase`)
        try c.encode(self.`lifecycle`, forKey: .`lifecycle`)
        try c.encode(self.`auth_state`, forKey: .`auth_state`)
        try c.encode(self.`sync_state`, forKey: .`sync_state`)
        try c.encode(self.`capabilities`, forKey: .`capabilities`)
        try c.encode(self.`scopes`, forKey: .`scopes`)
        try c.encode(self.`retry_at_ms`, forKey: .`retry_at_ms`)
        try c.encode(self.`trust_proposal`, forKey: .`trust_proposal`)
        try c.encode(self.`update_required`, forKey: .`update_required`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct HostsView: Codable {
    var `items`: [HostView]
    var `operations`: [Operation]
}

extension HostsView {
    private enum CodingKeys: String, CodingKey {
        case `items`
        case `operations`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`items` = try c.decode([HostView].self, forKey: .`items`)
        self.`operations` = try c.decode([Operation].self, forKey: .`operations`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`items`, forKey: .`items`)
        try c.encode(self.`operations`, forKey: .`operations`)
    }
}

struct OperationsView: Codable {
    var `items`: [Operation]
}

extension OperationsView {
    private enum CodingKeys: String, CodingKey {
        case `items`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`items` = try c.decode([Operation].self, forKey: .`items`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`items`, forKey: .`items`)
    }
}

struct HomeView: Codable {
    var `items`: [Pane]
    var `loading`: Bool
    var `stale`: Bool
    var `incomplete_scopes`: [String]
    var `error`: LocalError?
}

extension HomeView {
    private enum CodingKeys: String, CodingKey {
        case `items`
        case `loading`
        case `stale`
        case `incomplete_scopes`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`items` = try c.decode([Pane].self, forKey: .`items`)
        self.`loading` = try c.decode(Bool.self, forKey: .`loading`)
        self.`stale` = try c.decode(Bool.self, forKey: .`stale`)
        self.`incomplete_scopes` = try c.decode([String].self, forKey: .`incomplete_scopes`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`items`, forKey: .`items`)
        try c.encode(self.`loading`, forKey: .`loading`)
        try c.encode(self.`stale`, forKey: .`stale`)
        try c.encode(self.`incomplete_scopes`, forKey: .`incomplete_scopes`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct HistoryView: Codable {
    var `query`: String
    var `items`: [ThreadSummary]
    var `next_cursor`: String?
    var `loading`: Bool
    var `error`: LocalError?
}

extension HistoryView {
    private enum CodingKeys: String, CodingKey {
        case `query`
        case `items`
        case `next_cursor`
        case `loading`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`query` = try c.decode(String.self, forKey: .`query`)
        self.`items` = try c.decode([ThreadSummary].self, forKey: .`items`)
        self.`next_cursor` = try c.decodeIfPresent(String.self, forKey: .`next_cursor`)
        self.`loading` = try c.decode(Bool.self, forKey: .`loading`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`query`, forKey: .`query`)
        try c.encode(self.`items`, forKey: .`items`)
        try c.encode(self.`next_cursor`, forKey: .`next_cursor`)
        try c.encode(self.`loading`, forKey: .`loading`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct WorkspacesView: Codable {
    var `items`: [Workspace]
    var `loading`: Bool
    var `stale`: Bool
    var `error`: LocalError?
    var `history`: HistoryView
}

extension WorkspacesView {
    private enum CodingKeys: String, CodingKey {
        case `items`
        case `loading`
        case `stale`
        case `error`
        case `history`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`items` = try c.decode([Workspace].self, forKey: .`items`)
        self.`loading` = try c.decode(Bool.self, forKey: .`loading`)
        self.`stale` = try c.decode(Bool.self, forKey: .`stale`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
        self.`history` = try c.decode(HistoryView.self, forKey: .`history`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`items`, forKey: .`items`)
        try c.encode(self.`loading`, forKey: .`loading`)
        try c.encode(self.`stale`, forKey: .`stale`)
        try c.encode(self.`error`, forKey: .`error`)
        try c.encode(self.`history`, forKey: .`history`)
    }
}

struct HostsQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: HostsView?
    var `error`: LocalError?
}

extension HostsQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(HostsView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct OperationsQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: OperationsView?
    var `error`: LocalError?
}

extension OperationsQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(OperationsView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct HomeQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: HomeView?
    var `error`: LocalError?
}

extension HomeQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(HomeView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct WorkspacesQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: WorkspacesView?
    var `error`: LocalError?
}

extension WorkspacesQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(WorkspacesView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct RenderSpan: Codable {
    var `start`: UInt64
    var `end`: UInt64
    var `kind`: String
}

extension RenderSpan {
    private enum CodingKeys: String, CodingKey {
        case `start`
        case `end`
        case `kind`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`start` = try c.decode(UInt64.self, forKey: .`start`)
        self.`end` = try c.decode(UInt64.self, forKey: .`end`)
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`start`, forKey: .`start`)
        try c.encode(self.`end`, forKey: .`end`)
        try c.encode(self.`kind`, forKey: .`kind`)
    }
}

struct FileCitation: Codable {
    var `path`: String
    var `line`: UInt64? = nil
    var `end_line`: UInt64? = nil
}

extension FileCitation {
    private enum CodingKeys: String, CodingKey {
        case `path`
        case `line`
        case `end_line`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`path` = try c.decode(String.self, forKey: .`path`)
        if !c.contains(.`line`) { self.`line` = nil } else {
        self.`line` = try c.decodeIfPresent(UInt64.self, forKey: .`line`)
        }
        if !c.contains(.`end_line`) { self.`end_line` = nil } else {
        self.`end_line` = try c.decodeIfPresent(UInt64.self, forKey: .`end_line`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`path`, forKey: .`path`)
        try c.encode(self.`line`, forKey: .`line`)
        try c.encode(self.`end_line`, forKey: .`end_line`)
    }
}

struct MarkdownNode: Codable {
    var `kind`: String
    var `start`: UInt64
    var `end`: UInt64
    var `text`: String? = nil
    var `level`: UInt8? = nil
    var `ordered`: Bool? = nil
    var `url`: String? = nil
    var `language`: String? = nil
    var `children`: [MarkdownNode] = []
    var `citation`: FileCitation? = nil
}

extension MarkdownNode {
    private enum CodingKeys: String, CodingKey {
        case `kind`
        case `start`
        case `end`
        case `text`
        case `level`
        case `ordered`
        case `url`
        case `language`
        case `children`
        case `citation`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
        self.`start` = try c.decode(UInt64.self, forKey: .`start`)
        self.`end` = try c.decode(UInt64.self, forKey: .`end`)
        if !c.contains(.`text`) { self.`text` = nil } else {
        self.`text` = try c.decodeIfPresent(String.self, forKey: .`text`)
        }
        if !c.contains(.`level`) { self.`level` = nil } else {
        self.`level` = try c.decodeIfPresent(UInt8.self, forKey: .`level`)
        }
        if !c.contains(.`ordered`) { self.`ordered` = nil } else {
        self.`ordered` = try c.decodeIfPresent(Bool.self, forKey: .`ordered`)
        }
        if !c.contains(.`url`) { self.`url` = nil } else {
        self.`url` = try c.decodeIfPresent(String.self, forKey: .`url`)
        }
        if !c.contains(.`language`) { self.`language` = nil } else {
        self.`language` = try c.decodeIfPresent(String.self, forKey: .`language`)
        }
        if !c.contains(.`children`) { self.`children` = [] } else {
        self.`children` = try c.decode([MarkdownNode].self, forKey: .`children`)
        }
        if !c.contains(.`citation`) { self.`citation` = nil } else {
        self.`citation` = try c.decodeIfPresent(FileCitation.self, forKey: .`citation`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`start`, forKey: .`start`)
        try c.encode(self.`end`, forKey: .`end`)
        try c.encode(self.`text`, forKey: .`text`)
        try c.encode(self.`level`, forKey: .`level`)
        try c.encode(self.`ordered`, forKey: .`ordered`)
        try c.encode(self.`url`, forKey: .`url`)
        try c.encode(self.`language`, forKey: .`language`)
        try c.encode(self.`children`, forKey: .`children`)
        try c.encode(self.`citation`, forKey: .`citation`)
    }
}

struct MarkdownView: Codable {
    var `nodes`: [MarkdownNode]
}

extension MarkdownView {
    private enum CodingKeys: String, CodingKey {
        case `nodes`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`nodes` = try c.decode([MarkdownNode].self, forKey: .`nodes`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`nodes`, forKey: .`nodes`)
    }
}

struct HighlightView: Codable {
    var `spans`: [RenderSpan]
}

extension HighlightView {
    private enum CodingKeys: String, CodingKey {
        case `spans`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`spans` = try c.decode([RenderSpan].self, forKey: .`spans`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`spans`, forKey: .`spans`)
    }
}

struct DiffLine: Codable {
    var `kind`: String
    var `text`: String
    var `old_line`: UInt64? = nil
    var `new_line`: UInt64? = nil
    var `spans`: [RenderSpan] = []
}

extension DiffLine {
    private enum CodingKeys: String, CodingKey {
        case `kind`
        case `text`
        case `old_line`
        case `new_line`
        case `spans`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
        self.`text` = try c.decode(String.self, forKey: .`text`)
        if !c.contains(.`old_line`) { self.`old_line` = nil } else {
        self.`old_line` = try c.decodeIfPresent(UInt64.self, forKey: .`old_line`)
        }
        if !c.contains(.`new_line`) { self.`new_line` = nil } else {
        self.`new_line` = try c.decodeIfPresent(UInt64.self, forKey: .`new_line`)
        }
        if !c.contains(.`spans`) { self.`spans` = [] } else {
        self.`spans` = try c.decode([RenderSpan].self, forKey: .`spans`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`text`, forKey: .`text`)
        try c.encode(self.`old_line`, forKey: .`old_line`)
        try c.encode(self.`new_line`, forKey: .`new_line`)
        try c.encode(self.`spans`, forKey: .`spans`)
    }
}

struct DiffHunk: Codable {
    var `old_start`: UInt64
    var `old_count`: UInt64
    var `new_start`: UInt64
    var `new_count`: UInt64
    var `lines`: [DiffLine]
}

extension DiffHunk {
    private enum CodingKeys: String, CodingKey {
        case `old_start`
        case `old_count`
        case `new_start`
        case `new_count`
        case `lines`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`old_start` = try c.decode(UInt64.self, forKey: .`old_start`)
        self.`old_count` = try c.decode(UInt64.self, forKey: .`old_count`)
        self.`new_start` = try c.decode(UInt64.self, forKey: .`new_start`)
        self.`new_count` = try c.decode(UInt64.self, forKey: .`new_count`)
        self.`lines` = try c.decode([DiffLine].self, forKey: .`lines`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`old_start`, forKey: .`old_start`)
        try c.encode(self.`old_count`, forKey: .`old_count`)
        try c.encode(self.`new_start`, forKey: .`new_start`)
        try c.encode(self.`new_count`, forKey: .`new_count`)
        try c.encode(self.`lines`, forKey: .`lines`)
    }
}

struct DiffFile: Codable {
    var `old_path`: String?
    var `new_path`: String?
    var `binary`: Bool
    var `hunks`: [DiffHunk]
}

extension DiffFile {
    private enum CodingKeys: String, CodingKey {
        case `old_path`
        case `new_path`
        case `binary`
        case `hunks`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`old_path` = try c.decodeIfPresent(String.self, forKey: .`old_path`)
        self.`new_path` = try c.decodeIfPresent(String.self, forKey: .`new_path`)
        self.`binary` = try c.decode(Bool.self, forKey: .`binary`)
        self.`hunks` = try c.decode([DiffHunk].self, forKey: .`hunks`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`old_path`, forKey: .`old_path`)
        try c.encode(self.`new_path`, forKey: .`new_path`)
        try c.encode(self.`binary`, forKey: .`binary`)
        try c.encode(self.`hunks`, forKey: .`hunks`)
    }
}

struct DiffView: Codable {
    var `files`: [DiffFile]
}

extension DiffView {
    private enum CodingKeys: String, CodingKey {
        case `files`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`files` = try c.decode([DiffFile].self, forKey: .`files`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`files`, forKey: .`files`)
    }
}

struct MarkdownQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: MarkdownView?
    var `error`: LocalError?
}

extension MarkdownQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(MarkdownView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct HighlightQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: HighlightView?
    var `error`: LocalError?
}

extension HighlightQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(HighlightView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct DiffQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: DiffView?
    var `error`: LocalError?
}

extension DiffQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(DiffView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct DiffIndexEntry: Codable {
    var `path`: String
    var `additions`: UInt64
    var `deletions`: UInt64
    var `start`: UInt64
    var `end`: UInt64
    var `patch_start`: UInt64
}

extension DiffIndexEntry {
    private enum CodingKeys: String, CodingKey {
        case `path`
        case `additions`
        case `deletions`
        case `start`
        case `end`
        case `patch_start`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`path` = try c.decode(String.self, forKey: .`path`)
        self.`additions` = try c.decode(UInt64.self, forKey: .`additions`)
        self.`deletions` = try c.decode(UInt64.self, forKey: .`deletions`)
        self.`start` = try c.decode(UInt64.self, forKey: .`start`)
        self.`end` = try c.decode(UInt64.self, forKey: .`end`)
        self.`patch_start` = try c.decode(UInt64.self, forKey: .`patch_start`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`path`, forKey: .`path`)
        try c.encode(self.`additions`, forKey: .`additions`)
        try c.encode(self.`deletions`, forKey: .`deletions`)
        try c.encode(self.`start`, forKey: .`start`)
        try c.encode(self.`end`, forKey: .`end`)
        try c.encode(self.`patch_start`, forKey: .`patch_start`)
    }
}

struct DiffIndexView: Codable {
    var `files`: [DiffIndexEntry]
}

extension DiffIndexView {
    private enum CodingKeys: String, CodingKey {
        case `files`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`files` = try c.decode([DiffIndexEntry].self, forKey: .`files`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`files`, forKey: .`files`)
    }
}

struct DiffIndexQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: DiffIndexView?
    var `error`: LocalError?
}

extension DiffIndexQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(DiffIndexView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct Pane: Codable {
    var `id`: String
    var `workspace_id`: String
    var `kind`: String
    var `title`: String
    var `thread_id`: String? = nil
    var `terminal_id`: String? = nil
    var `status`: String = "idle"
    var `attention`: Bool = false
    var `started_at_ms`: Int64? = nil
    var `can_stop`: Bool = false
    var `attention_kind`: String? = nil
}

extension Pane {
    private enum CodingKeys: String, CodingKey {
        case `id`
        case `workspace_id`
        case `kind`
        case `title`
        case `thread_id`
        case `terminal_id`
        case `status`
        case `attention`
        case `started_at_ms`
        case `can_stop`
        case `attention_kind`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`id` = try c.decode(String.self, forKey: .`id`)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
        self.`title` = try c.decode(String.self, forKey: .`title`)
        if !c.contains(.`thread_id`) { self.`thread_id` = nil } else {
        self.`thread_id` = try c.decodeIfPresent(String.self, forKey: .`thread_id`)
        }
        if !c.contains(.`terminal_id`) { self.`terminal_id` = nil } else {
        self.`terminal_id` = try c.decodeIfPresent(String.self, forKey: .`terminal_id`)
        }
        if !c.contains(.`status`) { self.`status` = "idle" } else {
        self.`status` = try c.decode(String.self, forKey: .`status`)
        }
        if !c.contains(.`attention`) { self.`attention` = false } else {
        self.`attention` = try c.decode(Bool.self, forKey: .`attention`)
        }
        if !c.contains(.`started_at_ms`) { self.`started_at_ms` = nil } else {
        self.`started_at_ms` = try c.decodeIfPresent(Int64.self, forKey: .`started_at_ms`)
        }
        if !c.contains(.`can_stop`) { self.`can_stop` = false } else {
        self.`can_stop` = try c.decode(Bool.self, forKey: .`can_stop`)
        }
        if !c.contains(.`attention_kind`) { self.`attention_kind` = nil } else {
        self.`attention_kind` = try c.decodeIfPresent(String.self, forKey: .`attention_kind`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`id`, forKey: .`id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`title`, forKey: .`title`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`terminal_id`, forKey: .`terminal_id`)
        try c.encode(self.`status`, forKey: .`status`)
        try c.encode(self.`attention`, forKey: .`attention`)
        try c.encode(self.`started_at_ms`, forKey: .`started_at_ms`)
        try c.encode(self.`can_stop`, forKey: .`can_stop`)
        try c.encode(self.`attention_kind`, forKey: .`attention_kind`)
    }
}

struct ThreadSummary: Codable {
    var `workspace_id`: String
    var `thread_id`: String
    var `title`: String
    var `provider`: String
    var `model`: String?
    var `cwd`: String?
    var `open`: Bool
    var `archived`: Bool
    var `last_activity_at_ms`: Int64?
    var `status`: String
    var `history_bucket`: String
}

extension ThreadSummary {
    private enum CodingKeys: String, CodingKey {
        case `workspace_id`
        case `thread_id`
        case `title`
        case `provider`
        case `model`
        case `cwd`
        case `open`
        case `archived`
        case `last_activity_at_ms`
        case `status`
        case `history_bucket`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`title` = try c.decode(String.self, forKey: .`title`)
        self.`provider` = try c.decode(String.self, forKey: .`provider`)
        self.`model` = try c.decodeIfPresent(String.self, forKey: .`model`)
        self.`cwd` = try c.decodeIfPresent(String.self, forKey: .`cwd`)
        self.`open` = try c.decode(Bool.self, forKey: .`open`)
        self.`archived` = try c.decode(Bool.self, forKey: .`archived`)
        self.`last_activity_at_ms` = try c.decodeIfPresent(Int64.self, forKey: .`last_activity_at_ms`)
        self.`status` = try c.decode(String.self, forKey: .`status`)
        self.`history_bucket` = try c.decode(String.self, forKey: .`history_bucket`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`title`, forKey: .`title`)
        try c.encode(self.`provider`, forKey: .`provider`)
        try c.encode(self.`model`, forKey: .`model`)
        try c.encode(self.`cwd`, forKey: .`cwd`)
        try c.encode(self.`open`, forKey: .`open`)
        try c.encode(self.`archived`, forKey: .`archived`)
        try c.encode(self.`last_activity_at_ms`, forKey: .`last_activity_at_ms`)
        try c.encode(self.`status`, forKey: .`status`)
        try c.encode(self.`history_bucket`, forKey: .`history_bucket`)
    }
}

struct Workspace: Codable {
    var `workspace_id`: String
    var `label`: String
    var `path`: String
    var `open`: Bool
    var `panes`: [Pane]
    var `threads`: [ThreadSummary]
}

extension Workspace {
    private enum CodingKeys: String, CodingKey {
        case `workspace_id`
        case `label`
        case `path`
        case `open`
        case `panes`
        case `threads`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`label` = try c.decode(String.self, forKey: .`label`)
        self.`path` = try c.decode(String.self, forKey: .`path`)
        self.`open` = try c.decode(Bool.self, forKey: .`open`)
        self.`panes` = try c.decode([Pane].self, forKey: .`panes`)
        self.`threads` = try c.decode([ThreadSummary].self, forKey: .`threads`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`label`, forKey: .`label`)
        try c.encode(self.`path`, forKey: .`path`)
        try c.encode(self.`open`, forKey: .`open`)
        try c.encode(self.`panes`, forKey: .`panes`)
        try c.encode(self.`threads`, forKey: .`threads`)
    }
}

struct PushOpenKey: Codable {
    var `host_id`: String
    var `record_base64`: String
}

extension PushOpenKey {
    private enum CodingKeys: String, CodingKey {
        case `host_id`
        case `record_base64`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`host_id` = try c.decode(String.self, forKey: .`host_id`)
        self.`record_base64` = try c.decode(String.self, forKey: .`record_base64`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`host_id`, forKey: .`host_id`)
        try c.encode(self.`record_base64`, forKey: .`record_base64`)
    }
}

struct PushOpenRequest: Codable {
    var `api_version`: UInt32
    var `envelope`: String
    var `keys`: [PushOpenKey]
    var `recent`: [String]? = nil
}

extension PushOpenRequest {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `envelope`
        case `keys`
        case `recent`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`envelope` = try c.decode(String.self, forKey: .`envelope`)
        self.`keys` = try c.decode([PushOpenKey].self, forKey: .`keys`)
        if !c.contains(.`recent`) { self.`recent` = nil } else {
        self.`recent` = try c.decodeIfPresent([String].self, forKey: .`recent`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`envelope`, forKey: .`envelope`)
        try c.encode(self.`keys`, forKey: .`keys`)
        try c.encode(self.`recent`, forKey: .`recent`)
    }
}

struct PushNotification: Codable {
    var `api_version`: UInt32
    var `opened`: Bool
    var `update_required`: Bool
    var `error`: String?
    var `host_id`: String?
    var `workspace_id`: String?
    var `thread_id`: String?
    var `turn_id`: String?
    var `kind`: String
    var `attention`: String?
    var `channel`: String
    var `title`: String
    var `body`: String
    var `deep_link`: String
    var `actions`: [String]
    var `dedupe_key`: String?
    var `duplicate`: Bool
}

extension PushNotification {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `opened`
        case `update_required`
        case `error`
        case `host_id`
        case `workspace_id`
        case `thread_id`
        case `turn_id`
        case `kind`
        case `attention`
        case `channel`
        case `title`
        case `body`
        case `deep_link`
        case `actions`
        case `dedupe_key`
        case `duplicate`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`opened` = try c.decode(Bool.self, forKey: .`opened`)
        self.`update_required` = try c.decode(Bool.self, forKey: .`update_required`)
        self.`error` = try c.decodeIfPresent(String.self, forKey: .`error`)
        self.`host_id` = try c.decodeIfPresent(String.self, forKey: .`host_id`)
        self.`workspace_id` = try c.decodeIfPresent(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decodeIfPresent(String.self, forKey: .`thread_id`)
        self.`turn_id` = try c.decodeIfPresent(String.self, forKey: .`turn_id`)
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
        self.`attention` = try c.decodeIfPresent(String.self, forKey: .`attention`)
        self.`channel` = try c.decode(String.self, forKey: .`channel`)
        self.`title` = try c.decode(String.self, forKey: .`title`)
        self.`body` = try c.decode(String.self, forKey: .`body`)
        self.`deep_link` = try c.decode(String.self, forKey: .`deep_link`)
        self.`actions` = try c.decode([String].self, forKey: .`actions`)
        self.`dedupe_key` = try c.decodeIfPresent(String.self, forKey: .`dedupe_key`)
        self.`duplicate` = try c.decode(Bool.self, forKey: .`duplicate`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`opened`, forKey: .`opened`)
        try c.encode(self.`update_required`, forKey: .`update_required`)
        try c.encode(self.`error`, forKey: .`error`)
        try c.encode(self.`host_id`, forKey: .`host_id`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`turn_id`, forKey: .`turn_id`)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`attention`, forKey: .`attention`)
        try c.encode(self.`channel`, forKey: .`channel`)
        try c.encode(self.`title`, forKey: .`title`)
        try c.encode(self.`body`, forKey: .`body`)
        try c.encode(self.`deep_link`, forKey: .`deep_link`)
        try c.encode(self.`actions`, forKey: .`actions`)
        try c.encode(self.`dedupe_key`, forKey: .`dedupe_key`)
        try c.encode(self.`duplicate`, forKey: .`duplicate`)
    }
}

enum AttentionKind: String, Codable {
    case `unread`
    case `needs_approval`
    case `blocked`
    case `failed`
}

enum FileKind: String, Codable {
    case `file`
    case `preview`
}

enum AttentionStatus: String, Codable {
    case `idle`
    case `working`
    case `done`
    case `waiting`
    case `error`
}

struct AttentionItem: Codable {
    var `workspace_id`: String
    var `thread_id`: String
    var `turn_id`: String?
    var `kind`: AttentionKind
    var `status`: AttentionStatus
    var `since_ms`: Int64
    var `title`: String
    var `deep_link`: String
}

extension AttentionItem {
    private enum CodingKeys: String, CodingKey {
        case `workspace_id`
        case `thread_id`
        case `turn_id`
        case `kind`
        case `status`
        case `since_ms`
        case `title`
        case `deep_link`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`workspace_id` = try c.decode(String.self, forKey: .`workspace_id`)
        self.`thread_id` = try c.decode(String.self, forKey: .`thread_id`)
        self.`turn_id` = try c.decodeIfPresent(String.self, forKey: .`turn_id`)
        self.`kind` = try c.decode(AttentionKind.self, forKey: .`kind`)
        self.`status` = try c.decode(AttentionStatus.self, forKey: .`status`)
        self.`since_ms` = try c.decode(Int64.self, forKey: .`since_ms`)
        self.`title` = try c.decode(String.self, forKey: .`title`)
        self.`deep_link` = try c.decode(String.self, forKey: .`deep_link`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`turn_id`, forKey: .`turn_id`)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`status`, forKey: .`status`)
        try c.encode(self.`since_ms`, forKey: .`since_ms`)
        try c.encode(self.`title`, forKey: .`title`)
        try c.encode(self.`deep_link`, forKey: .`deep_link`)
    }
}

struct AttentionView: Codable {
    var `items`: [AttentionItem]
    var `count`: UInt32
    var `loading`: Bool
}

extension AttentionView {
    private enum CodingKeys: String, CodingKey {
        case `items`
        case `count`
        case `loading`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`items` = try c.decode([AttentionItem].self, forKey: .`items`)
        self.`count` = try c.decode(UInt32.self, forKey: .`count`)
        self.`loading` = try c.decode(Bool.self, forKey: .`loading`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`items`, forKey: .`items`)
        try c.encode(self.`count`, forKey: .`count`)
        try c.encode(self.`loading`, forKey: .`loading`)
    }
}

struct AttentionQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: AttentionView?
    var `error`: LocalError?
}

extension AttentionQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(AttentionView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ManageBusy: Codable {
    var `pending_turns`: UInt32 = 0
    var `running_tasks`: UInt32 = 0
}

extension ManageBusy {
    private enum CodingKeys: String, CodingKey {
        case `pending_turns`
        case `running_tasks`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`pending_turns`) { self.`pending_turns` = 0 } else {
        self.`pending_turns` = try c.decode(UInt32.self, forKey: .`pending_turns`)
        }
        if !c.contains(.`running_tasks`) { self.`running_tasks` = 0 } else {
        self.`running_tasks` = try c.decode(UInt32.self, forKey: .`running_tasks`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`pending_turns`, forKey: .`pending_turns`)
        try c.encode(self.`running_tasks`, forKey: .`running_tasks`)
    }
}

struct ManageJob: Codable {
    var `intent_id`: String
    var `kind`: String
    var `state`: String = "pending"
    var `workspace_id`: String? = nil
    var `thread_id`: String? = nil
    var `busy`: ManageBusy? = nil
    var `error`: LocalError? = nil
}

extension ManageJob {
    private enum CodingKeys: String, CodingKey {
        case `intent_id`
        case `kind`
        case `state`
        case `workspace_id`
        case `thread_id`
        case `busy`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`intent_id` = try c.decode(String.self, forKey: .`intent_id`)
        self.`kind` = try c.decode(String.self, forKey: .`kind`)
        if !c.contains(.`state`) { self.`state` = "pending" } else {
        self.`state` = try c.decode(String.self, forKey: .`state`)
        }
        if !c.contains(.`workspace_id`) { self.`workspace_id` = nil } else {
        self.`workspace_id` = try c.decodeIfPresent(String.self, forKey: .`workspace_id`)
        }
        if !c.contains(.`thread_id`) { self.`thread_id` = nil } else {
        self.`thread_id` = try c.decodeIfPresent(String.self, forKey: .`thread_id`)
        }
        if !c.contains(.`busy`) { self.`busy` = nil } else {
        self.`busy` = try c.decodeIfPresent(ManageBusy.self, forKey: .`busy`)
        }
        if !c.contains(.`error`) { self.`error` = nil } else {
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`intent_id`, forKey: .`intent_id`)
        try c.encode(self.`kind`, forKey: .`kind`)
        try c.encode(self.`state`, forKey: .`state`)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`thread_id`, forKey: .`thread_id`)
        try c.encode(self.`busy`, forKey: .`busy`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ManageDirectoryEntry: Codable {
    var `name`: String
    var `path`: String
}

extension ManageDirectoryEntry {
    private enum CodingKeys: String, CodingKey {
        case `name`
        case `path`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`name` = try c.decode(String.self, forKey: .`name`)
        self.`path` = try c.decode(String.self, forKey: .`path`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`name`, forKey: .`name`)
        try c.encode(self.`path`, forKey: .`path`)
    }
}

struct ManageDirectory: Codable {
    var `path`: String = ""
    var `parent`: String? = nil
    var `entries`: [ManageDirectoryEntry] = []
    var `suggestions`: [String] = []
    var `supported`: Bool = false
    var `loading`: Bool = false
    var `error`: LocalError? = nil
}

extension ManageDirectory {
    private enum CodingKeys: String, CodingKey {
        case `path`
        case `parent`
        case `entries`
        case `suggestions`
        case `supported`
        case `loading`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`path`) { self.`path` = "" } else {
        self.`path` = try c.decode(String.self, forKey: .`path`)
        }
        if !c.contains(.`parent`) { self.`parent` = nil } else {
        self.`parent` = try c.decodeIfPresent(String.self, forKey: .`parent`)
        }
        if !c.contains(.`entries`) { self.`entries` = [] } else {
        self.`entries` = try c.decode([ManageDirectoryEntry].self, forKey: .`entries`)
        }
        if !c.contains(.`suggestions`) { self.`suggestions` = [] } else {
        self.`suggestions` = try c.decode([String].self, forKey: .`suggestions`)
        }
        if !c.contains(.`supported`) { self.`supported` = false } else {
        self.`supported` = try c.decode(Bool.self, forKey: .`supported`)
        }
        if !c.contains(.`loading`) { self.`loading` = false } else {
        self.`loading` = try c.decode(Bool.self, forKey: .`loading`)
        }
        if !c.contains(.`error`) { self.`error` = nil } else {
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`path`, forKey: .`path`)
        try c.encode(self.`parent`, forKey: .`parent`)
        try c.encode(self.`entries`, forKey: .`entries`)
        try c.encode(self.`suggestions`, forKey: .`suggestions`)
        try c.encode(self.`supported`, forKey: .`supported`)
        try c.encode(self.`loading`, forKey: .`loading`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ManageNewChat: Codable {
    var `workspace_id`: String? = nil
    var `selection`: ChatSelection
    var `providers`: [ChatChoice] = []
    var `catalogs`: ChatCatalogs
    var `loading`: Bool = false
    var `can_create`: Bool = false
    var `error`: LocalError? = nil
}

extension ManageNewChat {
    private enum CodingKeys: String, CodingKey {
        case `workspace_id`
        case `selection`
        case `providers`
        case `catalogs`
        case `loading`
        case `can_create`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        if !c.contains(.`workspace_id`) { self.`workspace_id` = nil } else {
        self.`workspace_id` = try c.decodeIfPresent(String.self, forKey: .`workspace_id`)
        }
        self.`selection` = try c.decode(ChatSelection.self, forKey: .`selection`)
        if !c.contains(.`providers`) { self.`providers` = [] } else {
        self.`providers` = try c.decode([ChatChoice].self, forKey: .`providers`)
        }
        self.`catalogs` = try c.decode(ChatCatalogs.self, forKey: .`catalogs`)
        if !c.contains(.`loading`) { self.`loading` = false } else {
        self.`loading` = try c.decode(Bool.self, forKey: .`loading`)
        }
        if !c.contains(.`can_create`) { self.`can_create` = false } else {
        self.`can_create` = try c.decode(Bool.self, forKey: .`can_create`)
        }
        if !c.contains(.`error`) { self.`error` = nil } else {
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
        }
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`workspace_id`, forKey: .`workspace_id`)
        try c.encode(self.`selection`, forKey: .`selection`)
        try c.encode(self.`providers`, forKey: .`providers`)
        try c.encode(self.`catalogs`, forKey: .`catalogs`)
        try c.encode(self.`loading`, forKey: .`loading`)
        try c.encode(self.`can_create`, forKey: .`can_create`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}

struct ManageView: Codable {
    var `operations`: [ManageJob]
    var `directory`: ManageDirectory
    var `new_chat`: ManageNewChat
    var `can_manage_workspaces`: Bool
    var `can_create_threads`: Bool
}

extension ManageView {
    private enum CodingKeys: String, CodingKey {
        case `operations`
        case `directory`
        case `new_chat`
        case `can_manage_workspaces`
        case `can_create_threads`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`operations` = try c.decode([ManageJob].self, forKey: .`operations`)
        self.`directory` = try c.decode(ManageDirectory.self, forKey: .`directory`)
        self.`new_chat` = try c.decode(ManageNewChat.self, forKey: .`new_chat`)
        self.`can_manage_workspaces` = try c.decode(Bool.self, forKey: .`can_manage_workspaces`)
        self.`can_create_threads` = try c.decode(Bool.self, forKey: .`can_create_threads`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`operations`, forKey: .`operations`)
        try c.encode(self.`directory`, forKey: .`directory`)
        try c.encode(self.`new_chat`, forKey: .`new_chat`)
        try c.encode(self.`can_manage_workspaces`, forKey: .`can_manage_workspaces`)
        try c.encode(self.`can_create_threads`, forKey: .`can_create_threads`)
    }
}

struct ManageQuery: Codable {
    var `api_version`: UInt32
    var `revision`: String
    var `data`: ManageView?
    var `error`: LocalError?
}

extension ManageQuery {
    private enum CodingKeys: String, CodingKey {
        case `api_version`
        case `revision`
        case `data`
        case `error`
    }
    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.`api_version` = try c.decode(UInt32.self, forKey: .`api_version`)
        self.`revision` = try c.decode(String.self, forKey: .`revision`)
        self.`data` = try c.decodeIfPresent(ManageView.self, forKey: .`data`)
        self.`error` = try c.decodeIfPresent(LocalError.self, forKey: .`error`)
    }
    func encode(to encoder: Encoder) throws {
        var c = encoder.container(keyedBy: CodingKeys.self)
        try c.encode(self.`api_version`, forKey: .`api_version`)
        try c.encode(self.`revision`, forKey: .`revision`)
        try c.encode(self.`data`, forKey: .`data`)
        try c.encode(self.`error`, forKey: .`error`)
    }
}
